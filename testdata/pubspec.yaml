name: testdata
description: A new Flutter package project.
version: 0.0.1
author:
homepage:
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  decimal: ^0.3.0
  models:
#    path: ../models
    git:
      url: https://github.com/growerp/growerp.git
      ref: packages
      path: models

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

