<?xml version="1.0" encoding="UTF-8"?>
<entities xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/entity-definition-3.xsd">

    <!-- ========================================================= -->
    <!--  Marketing Persona (Customer Avatar)                      -->
    <!-- ========================================================= -->
    <entity entity-name="MarketingPersona" package="growerp.marketing">
        <description>Represents a target customer avatar/persona for AI content generation.</description>
        <field name="personaId" type="id" is-pk="true" />
        <field name="pseudoId" type="id" />
        <field name="ownerPartyId" type="id" />
        <field name="name" type="text-medium" />
        <field name="demographics" type="text-long" />
        <field name="painPoints" type="text-long" />
        <field name="goals" type="text-long" />
        <field name="toneOfVoice" type="text-long" />

        <field name="createdDate" type="date-time" />
        <field name="lastModifiedDate" type="date-time" />

        <relationship type="one" related="mantle.party.Party">
            <key-map field-name="ownerPartyId" related="partyId" />
        </relationship>
    </entity>

    <!-- ========================================================= -->
    <!--  Content Plan (Weekly PNP Schedule)                       -->
    <!-- ========================================================= -->
    <entity entity-name="ContentPlan" package="growerp.marketing">
        <description>Represents a weekly content plan based on the Pain-News-Prize formula.</description>
        <field name="planId" type="id" is-pk="true" />
        <field name="pseudoId" type="id" />
        <field name="ownerPartyId" type="id" />
        <field name="personaId" type="id" />
        <field name="weekStartDate" type="date-time" />
        <field name="theme" type="text-medium" />
        <field name="status" type="text-short" /> <!-- DRAFT, ACTIVE, COMPLETED -->

        <field name="createdDate" type="date-time" />
        <field name="lastModifiedDate" type="date-time" />

        <relationship type="one" related="growerp.marketing.MarketingPersona">
            <key-map field-name="personaId" />
        </relationship>
        <relationship type="one" related="mantle.party.Party">
            <key-map field-name="ownerPartyId" related="partyId" />
        </relationship>
    </entity>

    <!-- ========================================================= -->
    <!--  Social Post (Content Item)                               -->
    <!-- ========================================================= -->
    <entity entity-name="SocialPost" package="growerp.marketing">
        <description>Represents a specific social media post.</description>
        <field name="postId" type="id" is-pk="true" />
        <field name="pseudoId" type="id" />
        <field name="ownerPartyId" type="id" />
        <field name="planId" type="id" />
        <field name="type" type="text-short" /> <!-- PAIN, NEWS, PRIZE, OTHER -->
        <field name="platform" type="text-short" /> <!-- LINKEDIN, TWITTER, FACEBOOK, MEDIUM, SUBSTACK, EMAIL -->
        <field name="masterContentId" type="id" /> <!-- source MasterContent this was adapted from -->
        <field name="headline" type="text-medium" />
        <field name="draftContent" type="text-very-long" />
        <field name="finalContent" type="text-very-long" />
        <field name="status" type="text-short" /> <!-- DRAFT, READY, PUBLISHED -->
        <field name="scheduledDate" type="date-time" />
        <field name="publishedDate" type="date-time" />
        <field name="publishedUrl" type="text-medium" />
        <field name="publishError" type="text-long" />

        <field name="createdDate" type="date-time" />
        <field name="lastModifiedDate" type="date-time" />

        <relationship type="one" related="growerp.marketing.ContentPlan">
            <key-map field-name="planId" />
        </relationship>
        <relationship type="one" related="mantle.party.Party">
            <key-map field-name="ownerPartyId" related="partyId" />
        </relationship>
    </entity>

    <!-- ========================================================= -->
    <!--  Master Content (platform-agnostic source piece)         -->
    <!-- ========================================================= -->
    <entity entity-name="MasterContent" package="growerp.marketing">
        <description>Platform-neutral content authored once, then adapted per platform
            into SocialPost (broadcast) or OutreachMessage (1:1) children.</description>
        <field name="masterContentId" type="id" is-pk="true" />
        <field name="pseudoId" type="id" />
        <field name="ownerPartyId" type="id" />
        <field name="planId" type="id" /> <!-- optional link to weekly PNP ContentPlan -->
        <field name="contentType" type="text-short" /> <!-- POSTING, ARTICLE, MESSAGE -->
        <field name="pnpType" type="text-short" /> <!-- PAIN, NEWS, PRIZE, OTHER -->
        <field name="title" type="text-medium" />
        <field name="body" type="text-very-long" /> <!-- canonical platform-neutral copy -->
        <field name="callToAction" type="text-long" />
        <field name="targetUrl" type="text-medium" />
        <field name="status" type="text-short" /> <!-- DRAFT, APPROVED, ADAPTED -->

        <field name="createdDate" type="date-time" />
        <field name="lastModifiedDate" type="date-time" />

        <relationship type="one" related="growerp.marketing.ContentPlan">
            <key-map field-name="planId" />
        </relationship>
        <relationship type="one" related="mantle.party.Party">
            <key-map field-name="ownerPartyId" related="partyId" />
        </relationship>
    </entity>

    <!-- ========================================================= -->
    <!--  Appointment slots (public demo/consult booking)           -->
    <!-- ========================================================= -->
    <entity entity-name="AppointmentSlot" package="growerp.marketing">
        <description>A bookable time slot offered by the company; booking creates a
            WorkEffort event (workEffortId) with the lead as attendee.</description>
        <field name="slotId" type="id" is-pk="true" />
        <field name="ownerPartyId" type="id" />
        <field name="startDateTime" type="date-time" />
        <field name="endDateTime" type="date-time" />
        <field name="status" type="text-short" /> <!-- AVAILABLE, BOOKED -->
        <field name="workEffortId" type="id" />
        <relationship type="one" related="mantle.party.Party">
            <key-map field-name="ownerPartyId" related="partyId" />
        </relationship>
    </entity>
</entities>