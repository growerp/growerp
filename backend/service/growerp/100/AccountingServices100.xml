<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
-->
<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-definition-2.1.xsd">

    <service verb="init" noun="PartyAccountingConfiguration">
        <!-- partly copied from mantle-usl/service/mantle/ledger/LedgerServices.xml -->
        <in-parameters>
            <parameter name="sourcePartyId" required="true" />
            <parameter name="organizationPartyId" required="true" />
            <auto-parameters entity-name="mantle.ledger.config.PartyAcctgPreference" include="nonpk" />
            <parameter name="startYear" format="0000">
                <description>If specified creates fiscal time periods (year, quarter, month) starting that
                year and through the following year, otherwise starts now and creates current and next year, quarter, and month</description>
            </parameter>
        </in-parameters>
        <actions>
            <entity-find-one entity-name="mantle.ledger.config.PartyAcctgPreference"
                value-field="existingPartyAcctgPreference">
                <field-map field-name="organizationPartyId" />
            </entity-find-one>
            <if condition="existingPartyAcctgPreference">
                <return
                    message="Party [${organizationPartyId}] already has a PartyAcctgPreference record, so not initializing accounting configuration." />
            </if>

            <!-- create an error Journal mantle.ledger.transaction.GlJournal -->
            <entity-find-one entity-name="mantle.party.Organization" value-field="organization">
                <field-map field-name="partyId" from="organizationPartyId" />
            </entity-find-one>
            <set field="errorGlJournalId" value="${organizationPartyId}Error" />
            <service-call name="create#mantle.ledger.transaction.GlJournal"
                in-map="[organizationPartyId:organizationPartyId, glJournalId:errorGlJournalId,
                        glJournalTypeEnumId:'GLJRNL_ERROR', isPosted:'N',
                        glJournalName:('Error Journal for ' + (organization?.organizationName ?: organizationPartyId))]" />

            <!-- copy the mantle.ledger.config.PartyAcctgPreference -->
            <entity-find-one entity-name="mantle.ledger.config.PartyAcctgPreference"
                value-field="sourcePartyAcctgPreference">
                <field-map field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find-one>
            <if condition="!sourcePartyAcctgPreference">
                <return error="true"
                    message="Could not find PartyAcctgPreference for source party [${sourcePartyId}]" />
            </if>
            <set field="partyAcctgPreference" from="sourcePartyAcctgPreference.cloneValue()" />
            <!-- the fiscal year always starts on the first day of a quarter -->
            <if condition="fiscalYearStartMonth">
                <set field="fiscalYearStartDay" from="1" type="Integer" />
            </if>
            <entity-set value-field="partyAcctgPreference" include="nonpk" set-if-empty="false" />
            <service-call name="create#mantle.ledger.config.PartyAcctgPreference"
                in-map="partyAcctgPreference + [organizationPartyId:organizationPartyId, errorGlJournalId:errorGlJournalId]" />

            <!-- create new ledger from default settings -->
            <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                list="glAccounts">
                <econdition field-name="partyId" from="sourcePartyId" />
                <order-by field-name="glAccountId" />
            </entity-find>
            <set field="translate" from="[:]" />
            <iterate list="glAccounts" entry="glAccountOrg">
                <entity-find-one entity-name="mantle.ledger.account.GlAccount"
                    value-field="glAccount">
                    <field-map field-name="glAccountId" from="glAccountOrg.glAccountId" />
                </entity-find-one>
                <service-call name="create#mantle.ledger.account.GlAccount"
                    in-map="glAccount + [glAccountId: null,
                        accountCode: glAccountOrg.glAccountId,
                        parentGlAccountId: translate[glAccount.parentGlAccountId]]"
                    out-map="result" />
                <set field="translate[glAccount.glAccountId]" from="result.glAccountId" />
                <service-call name="create#mantle.ledger.account.GlAccountOrganization"
                    in-map="[glAccountId: result.glAccountId, organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- glAccount type default -->
            <entity-find entity-name="mantle.ledger.config.GlAccountTypeDefault" list="typeDefaults">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="typeDefaults" entry="typeDefault">
                <service-call name="create#mantle.ledger.config.GlAccountTypeDefault"
                    in-map="typeDefault + [glAccountId: translate[typeDefault.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- Invoice type -->
            <entity-find entity-name="mantle.ledger.config.InvoiceTypeTransType" list="invoiceTypes">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="invoiceTypes" entry="invoiceType">
                <service-call name="create#mantle.ledger.config.InvoiceTypeTransType"
                    in-map="invoiceType + [glAccountId: translate[invoiceType.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- Item type -->
            <entity-find entity-name="mantle.ledger.config.ItemTypeGlAccount" list="itemTypes">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="itemTypes" entry="itemType">
                <service-call name="create#mantle.ledger.config.ItemTypeGlAccount"
                    in-map="itemType + [glAccountId: translate[itemType.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- payment type -->
            <entity-find entity-name="mantle.ledger.config.PaymentTypeGlAccount" list="paymentTypes">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="paymentTypes" entry="paymentType">
                <service-call name="create#mantle.ledger.config.PaymentTypeGlAccount"
                    in-map="paymentType + [glAccountId: translate[paymentType.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- payment instrument -->
            <entity-find entity-name="mantle.ledger.config.PaymentInstrumentGlAccount"
                list="instrumentAccounts">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="instrumentAccounts" entry="instAccount">
                <service-call name="create#mantle.ledger.config.PaymentInstrumentGlAccount"
                    in-map="instAccount + [glAccountId: translate[instAccount.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- financial account -->
            <entity-find entity-name="mantle.ledger.config.FinancialAccountTypeGlAccount"
                list="finAccounts">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="finAccounts" entry="finAccount">
                <service-call name="create#mantle.ledger.config.FinancialAccountTypeGlAccount"
                    in-map="finAccount + [glAccountId: translate[finAccount.glAccountId],
                            organizationPartyId: organizationPartyId]" />
            </iterate>
            <!-- Asset type -->
            <entity-find entity-name="mantle.ledger.config.AssetTypeGlAccount" list="assetTypes">
                <econdition field-name="organizationPartyId" from="sourcePartyId" />
            </entity-find>
            <iterate list="assetTypes" entry="assetType">
                <service-call name="create#mantle.ledger.config.AssetTypeGlAccount"
                    in-map="assetType + [
                                assetTypeGlAccountId: null,
                                assetGlAccountId: translate[assetType.assetGlAccountId],
                                wipAssetGlAccountId: translate[assetType.wipAssetGlAccountId],
                                receiptGlAccountId: translate[assetType.receiptGlAccountId],
                                issuanceGlAccountId: translate[assetType.issuanceGlAccountId],
                                transferGlAccountId: translate[assetType.transferGlAccountId],
                                accDepreciationGlAccountId: translate[assetType.accDepreciationGlAccountId],
                                depreciationGlAccountId: translate[assetType.depreciationGlAccountId],
                                profitGlAccountId: translate[assetType.profitGlAccountId],
                                lossGlAccountId: translate[assetType.lossGlAccountId],
                                shrinkageGlAccountId: translate[assetType.shrinkageGlAccountId],
                                foundGlAccountId: translate[assetType.foundGlAccountId],
                                organizationPartyId: organizationPartyId]" />
            </iterate>

            <!-- setup mantle.party.time.TimePeriod for FiscalMonth, FiscalQuarter, FiscalYear -->
            <service-call name="growerp.100.AccountingServices100.create#FiscalTimePeriods"
                in-map="[organizationPartyId:organizationPartyId, startYear:startYear,
                        fiscalYearStartMonth:fiscalYearStartMonth]" />
        </actions>
    </service>

    <service verb="create" noun="FiscalTimePeriods">
        <description>Create the fiscal year, quarter and month periods for a company, starting at
            the fiscal year start month(1, 4, 7 or 10) of its accounting preference and running
            through the next fiscal year.</description>
        <in-parameters>
            <parameter name="organizationPartyId" required="true" />
            <parameter name="fiscalYearStartMonth" type="Integer">
                <description>1, 4, 7 or 10, when not provided taken from the PartyAcctgPreference</description>
            </parameter>
            <parameter name="startYear" format="0000">
                <description>Year of the first fiscal year to create, default the current fiscal year</description>
            </parameter>
            <parameter name="startDate" type="Date">
                <description>Alternative for startYear: start with the fiscal year containing this date</description>
            </parameter>
        </in-parameters>
        <actions>
            <if condition="!fiscalYearStartMonth">
                <entity-find-one entity-name="mantle.ledger.config.PartyAcctgPreference"
                    value-field="acctgPreference">
                    <field-map field-name="organizationPartyId" />
                </entity-find-one>
                <set field="fiscalYearStartMonth"
                    from="acctgPreference?.fiscalYearStartMonth ?: 1" type="Integer" />
            </if>
            <if condition="![1, 4, 7, 10].contains(fiscalYearStartMonth)">
                <return error="true"
                    message="Fiscal year start month: ${fiscalYearStartMonth} should be 1, 4, 7 or 10" />
            </if>

            <set field="today" from="java.time.LocalDate.now()" />
            <!-- the fiscal year we are in now: started this calendar year or the one before -->
            <set field="currentYearFrom"
                from="java.time.LocalDate.of(today.getMonthValue() &lt; fiscalYearStartMonth ?
                        today.getYear() - 1 : today.getYear(), fiscalYearStartMonth, 1)" />
            <set field="lastYearFrom" from="currentYearFrom.plusYears(1)" />
            <if condition="startYear">
                <then>
                    <set field="yearFrom"
                        from="java.time.LocalDate.of(startYear as int, fiscalYearStartMonth, 1)" />
                </then>
                <else-if condition="startDate">
                    <!-- the fiscal year containing the start date -->
                    <set field="startLocalDate" from="startDate.toLocalDate()" />
                    <set field="yearFrom"
                        from="java.time.LocalDate.of(startLocalDate.getMonthValue() &lt; fiscalYearStartMonth ?
                                startLocalDate.getYear() - 1 : startLocalDate.getYear(), fiscalYearStartMonth, 1)" />
                </else-if>
                <else><set field="yearFrom" from="currentYearFrom" /></else>
            </if>
            <!-- a start year further out than next year would otherwise create nothing -->
            <if condition="yearFrom.isAfter(lastYearFrom)">
                <set field="lastYearFrom" from="yearFrom" />
            </if>

            <set field="prevPeriodId" from="null" />
            <set field="prevQuarterId" from="null" />
            <set field="prevMonthId" from="null" />
            <while condition="!yearFrom.isAfter(lastYearFrom)">
                <set field="fromDate" from="java.sql.Date.valueOf(yearFrom)" />
                <service-call name="mantle.party.TimeServices.getOrCreate#TimePeriod"
                    out-map="curYearOut"
                    in-map="[partyId:organizationPartyId, timePeriodTypeId:'FiscalYear', fromDate:fromDate, previousPeriodId:prevPeriodId]" />
                <!-- create the quarters and months of this year one by one with their own
                    number: mantle get#DescendantPeriods starts a missing chain at the most
                    recent period of the type of the company, so for a year before that one
                    it creates nothing and the year is left without quarters and months -->
                <iterate list="[1, 2, 3, 4]" entry="quarterNum">
                    <set field="quarterFrom" from="yearFrom.plusMonths(((quarterNum - 1) * 3) as long)" />
                    <service-call name="mantle.party.TimeServices.getOrCreate#TimePeriod"
                        out-map="quarterOut"
                        in-map="[partyId:organizationPartyId, timePeriodTypeId:'FiscalQuarter',
                                fromDate:java.sql.Date.valueOf(quarterFrom),
                                parentPeriodId:curYearOut.timePeriodId, periodNum:quarterNum,
                                previousPeriodId:prevQuarterId]" />
                    <iterate list="[1, 2, 3]" entry="monthInQuarter">
                        <set field="monthFrom" from="quarterFrom.plusMonths((monthInQuarter - 1) as long)" />
                        <service-call name="mantle.party.TimeServices.getOrCreate#TimePeriod"
                            out-map="monthOut"
                            in-map="[partyId:organizationPartyId, timePeriodTypeId:'FiscalMonth',
                                    fromDate:java.sql.Date.valueOf(monthFrom),
                                    parentPeriodId:quarterOut.timePeriodId,
                                    periodNum:((quarterNum - 1) * 3) + monthInQuarter,
                                    previousPeriodId:prevMonthId]" />
                        <set field="prevMonthId" from="monthOut.timePeriodId" />
                    </iterate>
                    <set field="prevQuarterId" from="quarterOut.timePeriodId" />
                </iterate>

                <set field="prevPeriodId" from="curYearOut.timePeriodId" />
                <set field="yearFrom" from="yearFrom.plusYears(1)" />
            </while>
        </actions>
    </service>

    <service verb="check" noun="AcctgPeriodChangeAllowed">
        <description>The currency and the fiscal year start of a company can only be changed
            as long as nothing has been posted: no posted transactions and no closed periods.</description>
        <in-parameters>
            <parameter name="organizationPartyId" required="true" />
        </in-parameters>
        <out-parameters>
            <parameter name="allowed" type="Boolean" />
        </out-parameters>
        <actions>
            <entity-find entity-name="mantle.ledger.transaction.AcctgTrans" list="postedTrans"
                limit="1">
                <econdition field-name="organizationPartyId" />
                <econdition field-name="isPosted" value="Y" />
            </entity-find>
            <entity-find entity-name="mantle.party.time.TimePeriod" list="closedPeriods" limit="1">
                <econdition field-name="partyId" from="organizationPartyId" />
                <econdition field-name="isClosed" value="Y" />
            </entity-find>
            <set field="allowed" from="!postedTrans &amp;&amp; !closedPeriods" type="Boolean" />
        </actions>
    </service>

    <service verb="getOrCreate" noun="FiscalYearPeriod">
        <description>The fiscal year period of a company for a calendar year, created when it
            does not exist yet: the year can well be in the past.</description>
        <in-parameters>
            <parameter name="organizationPartyId" required="true" />
            <parameter name="year" format="0000" required="true" />
        </in-parameters>
        <out-parameters>
            <parameter name="timePeriodId" />
            <parameter name="fromDate" type="Date" />
            <parameter name="thruDate" type="Date" />
        </out-parameters>
        <actions>
            <entity-find-one entity-name="mantle.ledger.config.PartyAcctgPreference"
                value-field="acctgPreference">
                <field-map field-name="organizationPartyId" />
            </entity-find-one>
            <set field="fiscalYearStartMonth"
                from="acctgPreference?.fiscalYearStartMonth ?: 1" type="Integer" />
            <set field="yearFromDate"
                from="java.sql.Date.valueOf(java.time.LocalDate.of(year as int, fiscalYearStartMonth, 1))" />
            <!-- always run, the creation is by getOrCreate: a year created earlier can still
                miss quarters or months, which leaves the reports without their periods -->
            <service-call name="growerp.100.AccountingServices100.create#FiscalTimePeriods"
                in-map="[organizationPartyId: organizationPartyId, startYear: year,
                        fiscalYearStartMonth: fiscalYearStartMonth]" />
            <entity-find entity-name="mantle.party.time.TimePeriod" list="periods">
                <econdition field-name="partyId" from="organizationPartyId" />
                <econdition field-name="timePeriodTypeId" value="FiscalYear" />
                <econdition field-name="fromDate" from="yearFromDate" />
            </entity-find>
            <if condition="!periods">
                <return error="true"
                    message="Could not create the fiscal year ${year} for company ${organizationPartyId}" />
            </if>
            <set field="timePeriodId" from="periods[0].timePeriodId" />
            <set field="fromDate" from="periods[0].fromDate" />
            <set field="thruDate" from="periods[0].thruDate" />
        </actions>
    </service>

    <service verb="check" noun="InitialUploadAllowed">
        <description>The initial GlAccount upload replaces the whole ledger, so it is only
            allowed as long as the ledger holds nothing but the initial balance of an earlier
            upload. Note a failed import leaves its initial balance in the error journal, which
            has to be replaceable too, so match on the description instead of counting the
            transactions.</description>
        <in-parameters>
            <parameter name="organizationPartyId" required="true" />
        </in-parameters>
        <out-parameters>
            <parameter name="allowed" type="Boolean" />
            <parameter name="initialTransIds" type="List" />
            <parameter name="initialBalanceDescription">
                <description>Description marking the transaction as an initial balance, so the
                    caller creates its own initial balance with the same description</description>
            </parameter>
        </out-parameters>
        <actions>
            <set field="initialBalanceDescription" value="Initial balance" />
            <entity-find entity-name="mantle.ledger.transaction.AcctgTrans" list="otherTrans"
                limit="1">
                <econdition field-name="organizationPartyId" />
                <econditions combine="or">
                    <econdition field-name="description" operator="not-equals"
                        from="initialBalanceDescription" />
                    <econdition field-name="description" from="null" />
                </econditions>
                <select-field field-name="acctgTransId" />
            </entity-find>
            <set field="allowed" from="!otherTrans" type="Boolean" />
            <entity-find entity-name="mantle.ledger.transaction.AcctgTrans" list="initialTrans">
                <econdition field-name="organizationPartyId" />
                <econdition field-name="description" from="initialBalanceDescription" />
                <select-field field-name="acctgTransId" />
            </entity-find>
            <set field="initialTransIds" from="initialTrans.acctgTransId" />
        </actions>
    </service>

    <service verb="delete" noun="FiscalTimePeriods">
        <description>Remove all time periods of a company, newest first so the period chain
            and the parent periods are removed after their children.</description>
        <in-parameters>
            <parameter name="organizationPartyId" required="true" />
        </in-parameters>
        <actions>
            <entity-find entity-name="mantle.party.time.TimePeriod" list="periods">
                <econdition field-name="partyId" from="organizationPartyId" />
                <order-by field-name="-timePeriodId" />
            </entity-find>
            <iterate list="periods.timePeriodId" entry="id">
                <entity-delete-by-condition entity-name="mantle.party.time.TimePeriod">
                    <econdition field-name="partyId" from="organizationPartyId" />
                    <econdition field-name="timePeriodId" from="id" />
                </entity-delete-by-condition>
            </iterate>
        </actions>
    </service>

    <service verb="get" noun="TimePeriod">
        <description>Get the accounting periods valid for this company</description>
        <in-parameters>
            <parameter name="periodType" default-value="Y" /><!-- Y/Q/M -->
            <parameter name="year" /><!-- YYYY -->
        </in-parameters>
        <out-parameters>
            <parameter name="timePeriods" type="List">
                <parameter name="timePeriod" type="Map">
                    <parameter name="periodId" />
                    <parameter name="periodName" />
                    <parameter name="periodType" /><!-- Y/Q/M -->
                    <parameter name="fromDate" />
                    <parameter name="thruDate" />
                    <parameter name="isClosed" type="Boolean" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <set field="typeConvert"
                from="['Y':'FiscalYear','Q':'FiscalQuarter','M':'FiscalMonth',
                        'FiscalYear': 'Y','FiscalQuarter':'Q','FiscalMonth':'M']"
                type="NewMap" />
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if condition="year">
                <set field="year" value="%${year}%" />
            </if>
            <entity-find entity-name="growerp.party.time.TimePeriodChildAndType" list="periods">
                <econdition field-name="partyId" from="companyPartyId" />
                <econdition field-name="timePeriodTypeId" from="typeConvert[periodType]" />
                <econdition field-name="periodName" operator="like" from="year"
                    ignore-if-empty="true" />
                <order-by field-name="periodName" />
            </entity-find>
            <set field="timePeriods" from="[]" />
            <iterate list="periods" entry="p">
                <script>
                    timePeriods.add([periodId: p.timePeriodId,
                                    periodName: p.periodName,
                                    periodType: typeConvert[p.timePeriodTypeId],
                                    fromDate: p.fromDate.toString(),
                                    thruDate: p.thruDate.toString(),
                                    isClosed: p.isClosed == 'Y' ? true : false,
                                    hasPreviousPeriod: p.previousPeriodId != null ? true : false,
                                    hasNextPeriod: p.nextPeriodId != null ? true : false,
                                    ]); 
                </script>
            </iterate>
        </actions>
    </service>

    <service verb="update" noun="TimePeriod">
        <description>add, update and delete timeperiods</description>
        <in-parameters>
            <parameter name="createNext" type="Boolean" />
            <parameter name="createPrevious" type="Boolean" />
            <parameter name="delete" type="Boolean" />
            <parameter name="timePeriodId" />
        </in-parameters>
        <out-parameters>
            <parameter name="timePeriods" type="List">
                <parameter name="timePeriod" type="Map">
                    <parameter name="periodId" />
                    <parameter name="periodName" />
                    <parameter name="fromDate" />
                    <parameter name="thruDate" />
                    <parameter name="isClosed" type="Boolean" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if condition="createNext || createPrevious || delete">
                <if condition="!timePeriodId">
                    <return error="true"
                        message="Current periodId required when create previous or next provided" />
                    <else>
                        <!-- check if period is owned by the current company-->
                        <entity-find-one entity-name="mantle.party.time.TimePeriod"
                            value-field="timePeriod">
                            <field-map field-name="partyId" from="companyPartyId" />
                            <field-map field-name="timePeriodId" from="timePeriodId" />
                        </entity-find-one>
                        <if condition="!timePeriod">
                            <return error="true"
                                message="TimePeriodId: $periodId not found for company party id: $companyPartyId" />
                        </if>
                        <if condition="createPrevious">
                            <service-call name="mantle.party.TimeServices.create#TimePeriod"
                                in-map="[nextPeriodId: timePeriod.timePeriodId]" />
                        </if>
                        <if condition="createNext">
                            <service-call name="mantle.party.TimeServices.create#TimePeriod"
                                in-map="[previousPeriodId: timePeriod.timePeriodId]" />
                        </if>
                        <if condition="delete">
                            <service-call name="mantle.party.TimeServices.delete#TimePeriod"
                                in-map="[timePeriodId: timePeriodId]" />
                        </if>
                    </else>
                </if>
            </if>
            <service-call name="growerp.100.AccountingServices100.get#TimePeriod"
                out-map="context" />
        </actions>
    </service>

    <service verb="close" noun="TimePeriod">
        <description>add, update and delete timeperiods</description>
        <in-parameters>
            <parameter name="timePeriodId" />
        </in-parameters>
        <out-parameters>
            <parameter name="timePeriods" type="List">
                <parameter name="timePeriod" type="Map">
                    <parameter name="periodId" />
                    <parameter name="periodName" />
                    <parameter name="fromDate" />
                    <parameter name="thruDate" />
                    <parameter name="isClosed" type="Boolean" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <service-call name="growerp.100.AccountingServices100.get#TimePeriod"
                out-map="context" />
            <entity-find-one entity-name="mantle.party.time.TimePeriod"
                value-field="timePeriod">
                <field-map field-name="partyId" from="companyPartyId" />
                <field-map field-name="timePeriodId" from="timePeriodId" />
            </entity-find-one>
            <if condition="!timePeriod">
                <return error="true"
                    message="TimePeriodId: $periodId not found for company party id: $companyPartyId" />
            </if>
            <service-call name="growerp.100.AccountingServices100.close#FinancialTimePeriod"
                in-map="[timePeriodId: timePeriodId, closeChildren: 'Y']" />
            <service-call name="growerp.100.AccountingServices100.get#TimePeriod"
                out-map="context" />
        </actions>
    </service>

    <service verb="get" noun="BalanceSummary">
        <in-parameters>
            <parameter name="periodName" required="true" />
        </in-parameters>
        <out-parameters>
            <parameter name="ledgerReport" type="Map">
                <parameter name="company" type="Map">
                    <parameter name="companyId" />
                    <parameter name="companyName" />
                </parameter>
                <parameter name="title" />
                <parameter name="printDate" />
                <parameter name="timePeriod" type="Map">
                    <parameter name="periodId" />
                    <parameter name="periodName" />
                    <parameter name="fromDate" />
                    <parameter name="thruDate" />
                    <parameter name="isClosed" type="Boolean" />
                </parameter>
                <parameter name="glAccounts" type="List">
                    <parameter name="accountCode" />
                    <parameter name="accountName" />
                    <parameter name="level" />
                    <parameter name="beginningBalance" />
                    <parameter name="postedDebits" />
                    <parameter name="postedCredits" />
                    <parameter name="postedBalance" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <entity-find entity-name="mantle.party.time.TimePeriod" list="periods">
                <econdition field-name="partyId" from="companyPartyId" />
                <econdition field-name="periodName" from="periodName" />
            </entity-find>
            <if condition="!periods">
                <log message="Period $periodName not found!" />
                <entity-find entity-name="mantle.party.time.TimePeriod" list="periods">
                    <econdition field-name="partyId" from="companyPartyId" />
                    <order-by field-name="-periodName" />
                </entity-find>
                <if condition="!periods">
                    <return error="true" message="Balance summary: no periods found" />
                    <else>
                        <set field="periodName" from="periods[0].periodName" />
                        <log message="Using most recent period: $periodName" />
                    </else>
                </if>
            </if>
            <entity-find entity-name="mantle.ledger.report.GlAccountOrgTimePeriodDetail"
                list="postedBalanceSummary" limit="150">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="periodName" from="periodName" />
                <econditions combine="or">
                    <econdition field-name="beginningBalance" operator="not-equals"
                        from="BigDecimal.ZERO" />
                    <econdition field-name="endingBalance" operator="not-equals"
                        from="BigDecimal.ZERO" />
                    <econdition field-name="postedDebits" operator="not-equals"
                        from="BigDecimal.ZERO" />
                    <econdition field-name="postedCredits" operator="not-equals"
                        from="BigDecimal.ZERO" />
                </econditions>
                <select-field
                    field-name="accountCode,accountName,beginningBalance,postedDebits,postedCredits,endingBalance" />
                <order-by field-name="accountCode" />
            </entity-find>
            <entity-find-one entity-name="mantle.party.Organization" value-field="company">
                <field-map field-name="partyId" from="companyPartyId" />
            </entity-find-one>
            <set field="typeConvert"
                from="['Y':'FiscalYear','Q':'FiscalQuarter','M':'FiscalMonth',
                    'FiscalYear': 'Y','FiscalQuarter':'Q','FiscalMonth':'M']"
                type="NewMap" />
            <set field="ledgerReport"
                from="[ period: [periodId: periods[0].timePeriodId,
                                    periodName: periods[0].periodName,
                                    periodType: typeConvert[periods[0].timePeriodTypeId],
                                    fromDate: periods[0].fromDate.toString(),
                                    thruDate: periods[0].thruDate.toString(),
                                    isClosed: periods[0].isClosed == 'Y' ? true : false,
                                    ], company: [companyPartyId: companyPartyId,
                companyName: company.organizationName],
                title: 'Balance Summary report',
                printDate: ec.user.nowTimestamp.toString(),glAccounts: []]" />
            <set field="glAccountCodeMask"
                from="partyAcctgPreference?.glAccountCodeMask ?: ec.user.getPreference('GlAccountCodeMask')" />
            <set field="accountCodeFormatter" from="masker(glAccountCodeMask, '0')" />
            <script>
                //change values into strings
                for (el in postedBalanceSummary) {
                    def temp = [beginningBalance: el.beginningBalance?.toString(),
                                postedBalance: el.endingBalance?.toString(),
                                postedCredits: el.postedCredits?.toString(),
                                postedDebits: el.postedDebits?.toString(),
                                accountCode: accountCodeFormatter != null ? accountCodeFormatter.valueToString(el.accountCode) : el.accountCode,
                                accountName: ec.l10n.localize(el.accountName),
                                ];
                    ledgerReport.glAccounts.add(temp);
                }
            </script>
        </actions>
    </service>

    <service verb="recalculate" noun="GlAccountOrgSummaries">
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <!-- include userlogin for notification -->
            <service-call in-map="organizationPartyId: companyPartyId" async="true"
                include-user-login="true"
                name="mantle.ledger.LedgerServices.recalculate#GlAccountOrgSummaries" />
        </actions>
    </service>

    <service verb="get" noun="BalanceSheet">
        <in-parameters>
            <parameter name="periodName" required="true" />
        </in-parameters>
        <out-parameters>
            <parameter name="ledgerReport" type="Map">
                <parameter name="company" type="Map">
                    <parameter name="companyId" />
                    <parameter name="companyName" />
                </parameter>
                <parameter name="title" />
                <parameter name="printDate" />
                <parameter name="timePeriod" type="Map">
                    <parameter name="periodId" />
                    <parameter name="periodName" />
                    <parameter name="periodType" />
                    <parameter name="fromDate" />
                    <parameter name="thruDate" />
                    <parameter name="isClosed" type="Boolean" />
                </parameter>
                <parameter name="glAccounts" type="List">
                    <parameter name="glAccount" type="Map">
                        <parameter name="accountCode" />
                        <parameter name="accountName" />
                        <parameter name="level" />
                        <parameter name="beginningBalance" />
                        <parameter name="postedDebits" />
                        <parameter name="postedCredits" />
                        <parameter name="postedBalance" />
                    </parameter>
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <entity-find entity-name="mantle.party.time.TimePeriod" list="periods">
                <econdition field-name="partyId" from="companyPartyId" />
                <econdition field-name="periodName" from="periodName" />
            </entity-find>
            <if condition="!periods">
                <return error="true" message="Balance sheet, period: $periodName not found" />
            </if>
            <service-call
                name="mantle.ledger.LedgerReportServices.run#BalanceSheet"
                in-map="[organizationPartyId: companyPartyId, 
                                    timePeriodIdList: [periods[0].timePeriodId]]"
                out-map="context" />
            <set field="glAccountCodeMask"
                from="partyAcctgPreference?.glAccountCodeMask ?: ec.user.getPreference('GlAccountCodeMask')" />
            <set field="accountCodeFormatter" from="masker(glAccountCodeMask, '0')" />
            <script>
                def currencyFormat = currencyFormat ?: "#,##0.00";

                def showClass(Map classInfo, int depth) {
                    // if (!classInfo.totalBalanceByTimePeriod.ALL &amp;&amp; !classInfo.totalPostedByTimePeriod.ALL) return null;
                    //ec.logger.info("==$depth-1==${classInfo.glAccountClassEnumId}/${classInfo.className} =======${classInfo.totalBalanceByTimePeriod.ALL}")
                    def classInfoData = [level: depth, children: []];
                    classInfo.each { classInfoEl -&gt;
                        switch(classInfoEl.key) {
                            case 'className':
                                classInfoData.accountName = ec.l10n.localize(classInfoEl.value);
                                break;
                            case 'glAccountClassEnumId':
                                classInfoData.accountCode = classInfoEl.value
                                break;
                            case 'totalBalanceByTimePeriod':
                                if (!classInfoEl.value) return;
                                classInfoData.postedBalance = classInfoEl.value.ALL.toString();
                                break;
                            case 'glAccountDetailList':
                                classInfoEl.value.each {
                                    if (it.endingBalance.compareTo(BigDecimal.ZERO) != 0) { // not equal 0
                                        // ec.logger.info("==dept: $depth-2==${it.accountCode}/${it.accountName} ${it.endingBalance}");
                                        def newGl = [
                                                level: depth+1,
                                                accountCode: accountCodeFormatter != null ? accountCodeFormatter.valueToString(it.accountCode) : it.accountCode,
                                                accountName: ec.l10n.localize(it.accountName),
                                                postedBalance: it.endingBalance.toString(),
                                        ];
                                        classInfoData.children.add(newGl);
                                    }
                                }
                                break;
                            case 'childClassInfoList':
                                classInfoEl.value.each {
                                    def result = showClass(it, depth + 1);
                                    if (result) {
                                        classInfoData.children.add(result);
                                    }
                                };
                                break;
                        }
                    }
                    return classInfoData;
                }
                if(classInfoById.ASSET) asset = showClass(classInfoById.ASSET, 1);
                if(classInfoById.LIABILITY) liability = showClass(classInfoById.LIABILITY, 1);
                if(classInfoById.EQUITY) equity = showClass(classInfoById.EQUITY, 1);
                if(classInfoById.DISTRIBUTION) distribution = showClass(classInfoById.DISTRIBUTION, 1);
                def incomeAmount = [accountCode: 'INCOME', postedBalance: netIncomeOut.totalBalance.ALL.toString()];
            </script>
            <set field="typeConvert"
                from="['Y':'FiscalYear','Q':'FiscalQuarter','M':'FiscalMonth',
                    'FiscalYear': 'Y','FiscalQuarter':'Q','FiscalMonth':'M']"
                type="NewMap" />
            <set field="ledgerReport"
                from="[
                period: [periodId: periods[0].timePeriodId,
                                    periodName: periods[0].periodName,
                                    periodType: typeConvert[periods[0].timePeriodTypeId],
                                    fromDate: periods[0].fromDate.toString(),
                                    thruDate: periods[0].thruDate.toString(),
                                    isClosed: periods[0].isClosed == 'Y' ? true : false,
                                    ],
                title: 'Balance Sheet', printDate: ec.user.nowTimestamp.toString(),
                glAccounts: [asset?:[:], liability?:[:], equity?:[:], distribution?:[:], incomeAmount.totalBalance?.ALL?incomeAmount:[:]]]" />
        </actions>
    </service>

    <service verb="get" noun="OperatingRevenueExpenseChart">
        <description>Create data for the above report</description>
        <in-parameters>
            <parameter name="periodName" required="true" /> <!-- Ynnnn[Q,M]-->
        </in-parameters>
        <out-parameters><!-- need to be adjusted -->
            <parameter name="ledgerReport" type="Map">
                <parameter name="company" type="Map">
                    <parameter name="companyId" />
                    <parameter name="companyName" />
                </parameter>
                <parameter name="title" />
                <parameter name="printDate" />
                <parameter name="csvRows" type="List">
                    <parameter name="cells" type="List">
                        <parameter name="cell" />
                    </parameter>
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <set field="typeConvert"
                from="['Y':'FiscalYear','q':'FiscalQuarter','m':'FiscalMonth',
                    'FiscalYear': 'Y','FiscalQuarter':'Q','FiscalMonth':'M']"
                type="NewMap" />
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <entity-find entity-name="mantle.party.time.TimePeriod" list="timePeriods">
                <econdition field-name="partyId" from="companyPartyId" />
                <econdition field-name="periodName" from="periodName" />
            </entity-find>
            <if condition="!timePeriods">
                <return error="true"
                    message="Revenue/Expense sheet, period: $periodName not found for company $companyPartyId" />
            </if>
            <script>
                datasets = [
                [classGroupEnumId:'EngGlcNetRevenue'],
                [label:'Cost of Sales', classes:['COST_OF_SALES'], stack:'Expense'],
                [label:'Sales Expense', classes:['SALES_EXPENSE'], stack:'Expense'],
                [label:'Gen/Admin Expense', classes:['GEN_ADMIN_EXPENSE'], stack:'Expense'],
                [label:'Other Expense', classes:['CONTRIBUTIONS', 'NON_CASH_EXPENSE'],
                stack:'Expense'],
                [classGroupEnumId:'EngGlcNetOpIncome', type:'line', fill:false]
                ]
            </script>
            <!-- a period name is Ynnnn for a year, Ynnnnqnn or Ynnnnmnn for a quarter or a
                month: the character telling the type apart is the one after the year, the
                first character is a Y for all three -->
            <set field="periodTypeChar"
                from="periodName.length() &gt; 5 ? periodName.substring(5, 6) : 'Y'" />
            <!-- the chart runs by month: report the fiscal months the requested period is made
                up of, 12 for a year, 3 for a quarter and the month itself for a month -->
            <set field="monthCount"
                from="periodTypeChar == 'q' ? 3 : (periodTypeChar == 'm' ? 1 : 12)"
                type="Integer" />
            <service-call name="mantle.party.TimeServices.find#PartyTimePeriodGroups"
                out-map="periodsOut"
                in-map="[partyId:companyPartyId,
                        timePeriodTypeId: 'FiscalMonth',
                        thruDate: timePeriods[0].thruDate,
                        groups:(periods?:monthCount)]" />
            <if condition="periodsOut.timePeriodGroups">
                <service-call name="mantle.ledger.LedgerReportServices.get#PeriodSeriesSummary"
                    out-map="context"
                    in-map="[organizationPartyId:companyPartyId,
                            timePeriodGroups:periodsOut.timePeriodGroups,
                            datasets:datasets]" />

                <service-call name="mantle.ledger.LedgerReportServices.get#PeriodSeriesSummaryRows"
                    out-map="context" in-map="context" />
                <set field="revenueExpenseCsv" from="rowsList.collect({ it.join(',') }).join('\n')" />
                <set field="csvRows" from="[]" />
                <iterate list="rowsList" entry="row">
                    <set field="csvCells" from="[]" />
                    <iterate list="row" entry="cell">
                        <script>csvCells.add(cell.toString().replaceAll('-',''))</script>
                    </iterate>
                    <script>csvRows.add(csvCells)</script>
                </iterate>
            </if>
            <set field="ledgerReport"
                from="[
                company: [partyId: companyPartyId],
                period: [periodId: timePeriods[0].timePeriodId,
                                    periodName: timePeriods[0].periodName,
                                    periodType: typeConvert[timePeriods[0].timePeriodTypeId],
                                    fromDate: timePeriods[0].fromDate.toString(),
                                    thruDate: timePeriods[0].thruDate.toString(),
                                    isClosed: timePeriods[0].isClosed == 'Y' ? true : false,
                                    ],
                printDate: ec.user.nowTimestamp.toString(),
                csvRows: csvRows]" />
        </actions>
    </service>

    <service verb="get" noun="Ledger">
        <out-parameters>
            <parameter name="ledgerReport" type="Map">
                <parameter name="company" type="Map">
                    <parameter name="companyId" />
                    <parameter name="companyName" />
                </parameter>
                <parameter name="title" />
                <parameter name="printDate" />
                <parameter name="glAccounts" type="List">
                    <parameter name="glAccount" type="Map">
                        <parameter name="glAccountId" />
                        <parameter name="accountCode" />
                        <parameter name="accountName" />
                        <parameter name="level" />
                        <parameter name="beginningBalance" />
                        <parameter name="postedDebits" />
                        <parameter name="postedCredits" />
                        <parameter name="postedBalance" />
                    </parameter>
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <!--service-call
            in-map="organizationPartyId: companyPartyId"
                name="mantle.ledger.LedgerServices.recalculate#GlAccountOrgSummaries"/-->
            <entity-find list="dbGlAccounts"
                entity-name="growerp.ledger.account.GlAccountAndOrganization">
                <econdition field-name="partyId" from="companyPartyId" />
                <order-by field-name="accountCode" />
            </entity-find>
            <entity-find-one entity-name="mantle.party.Organization" value-field="company">
                <field-map field-name="partyId" from="companyPartyId" />
            </entity-find-one>
            <set field="ledgerReport"
                from="[
                company: [companyPartyId: companyPartyId,
                companyName: company.organizationName],
                title: 'Current Ledger Tree report',
                printDate: ec.user.nowTimestamp.toString(),
                glAccounts: []]" />
            <set field="glAccountCodeMask"
                from="partyAcctgPreference?.glAccountCodeMask ?: ec.user.getPreference('GlAccountCodeMask')" />
            <set field="accountCodeFormatter" from="masker(glAccountCodeMask, '0')" />
            <script>
                def getGlAccountTree(Map dbGlAccount, int dept) {
                    // ec.logger.info("===accountCode: ${dbGlAccount.accountCode} posted: ${dbGlAccount.postedBalance}")
                    def glAccount = [:];
                    glAccount.level = dept;
                    // glAccount.accountCode = accountCodeFormatter != null ? accountCodeFormatter.valueToString(dbGlAccount.accountCode) : dbGlAccount.accountCode;
                    glAccount.isDebit = dbGlAccount.isDebit == 'Y' ? true : false;
                    glAccount.accountName = ec.l10n.localize(dbGlAccount.accountName);
                    glAccount.postedBalance = dbGlAccount.postedBalance ? dbGlAccount.postedBalance.toString() : '0';
                    glAccount.rollUp = dbGlAccount.postedBalance ? dbGlAccount.postedBalance.toString() : '0';
                    glAccount.classId = dbGlAccount.glAccountClassEnumId;
                    glAccount.glAccountId = dbGlAccount.glAccountId;
                    glAccount.accountCode = dbGlAccount.accountCode;
                    def dependents = dbGlAccounts.findAll { 
                        dbGlAccount.glAccountId == it.parentGlAccountId
                    }
                    glAccount.children = [];
                    dependents.each {
                        def glAccountSave = getGlAccountTree(it, dept + 1)
                        def rollUp = new BigDecimal(glAccount.rollUp ?: '0').add(new BigDecimal(glAccountSave.rollUp ?: '0'))
                        glAccount.rollUp = rollUp? rollUp.toString() : '0';
                        glAccount.children.add(glAccountSave)
                    }
                    return glAccount;
                }

                def maskLen = glAccountCodeMask?.count('#') ?: 0
                def rootAccounts = dbGlAccounts.findAll {
                    it.parentGlAccountId == null &amp;&amp; (maskLen == 0 || it.accountCode?.length() == maskLen) }
                rootAccounts.each {
                 //   if(it.accountCode == '70000') {
                 //       ec.logger.info("====tree ${it.accountCode}: ${getGlAccountTree(it, 1)}")
                        ledgerReport.glAccounts.add(getGlAccountTree(it, 1));
                 //   }
                }
            </script>
        </actions>
    </service>

    <service verb="get" noun="ItemType">
        <description>itemtypes for orderItem and invoiceItem either sales or purchase</description>
        <in-parameters>
            <parameter name="sales" type="Boolean" />
        </in-parameters>
        <out-parameters>
            <parameter name="itemTypes" type="List">
                <parameter name="itemType" type="Map">
                    <parameter name="itemTypeId" />
                    <parameter name="itemTypeName" />
                    <parameter name="accountCode" />
                    <parameter name="accountName" />
                    <parameter name="direction" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if condition="sales != null">
                <set field="direction" from="sales == true? 'O' : 'I'" />
            </if>
            <entity-find entity-name="moqui.basic.EnumerationAndType" list="enumItemTypes">
                <econdition field-name="enumTypeId" value="ItemType" />
                <order-by field-name="description" />
            </entity-find>
            <entity-find entity-name="mantle.ledger.config.ItemTypeAndGlAccount" list="itemTypeList">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="direction" from="direction" ignore-if-empty="true" />
                <order-by field-name="[itemTypeDescription,accountCode]" />
            </entity-find>
            <set field="itemTypes" from="[]" />
            <iterate list="['I','O']" entry="inOut">
                <iterate list="enumItemTypes" entry="itemType">
                    <script>
                        var newItemType = itemTypeList.find{
                            item -&gt; item.itemTypeEnumId == itemType.enumId
                                &amp;&amp; item.direction == inOut
                            }
                    </script>
                    <if
                        condition="(sales != null &amp;&amp; newItemType != null) || sales == null">
                        <set field="itemTypeOut"
                            from="[itemTypeId: itemType.enumId,
                            itemTypeName: itemType.description,
                            accountCode: newItemType?.accountCode,
                            accountName: newItemType?.accountName ? ec.l10n.localize(newItemType.accountName) : null,
                            direction: inOut,
                        ]" />
                        <script>itemTypes.add(itemTypeOut)</script>
                    </if>
                </iterate>
                <script>itemTypes.sort{it.itemTypeName + it.direction}</script>
            </iterate>
        </actions>
    </service>

    <service verb="update" noun="ItemType">
        <description>itemtypes for orderItem and invoiceItem either sales or purchase</description>
        <in-parameters>
            <parameter name="itemType" type="Map">
                <parameter name="itemTypeId" />
                <parameter name="itemTypeName" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="direction" />
            </parameter>
            <parameter name="update" type="Boolean" />
            <parameter name="delete" type="Boolean" />
        </in-parameters>
        <out-parameters>
            <parameter name="itemType" type="Map">
                <parameter name="itemTypeId" />
                <parameter name="itemTypeName" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="direction" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if
                condition="itemType.itemTypeId &amp;&amp; itemType.direction &amp;&amp; itemType.accountCode">
                <entity-find-one entity-name="mantle.ledger.config.ItemTypeGlAccount"
                    value-field="itemTypeGlAccount" for-update="true">
                    <field-map field-name="itemTypeEnumId" from="itemType.itemTypeId" />
                    <field-map field-name="organizationPartyId" from="companyPartyId" />
                    <field-map field-name="direction" from="itemType.direction" />
                </entity-find-one>
                <if condition="!itemTypeGlAccount &amp;&amp; delete">
                    <return error="true"
                        message="Could not find itemTypeId/company/direction ${itemType.itemTypeId}/${companyPartyId}/${itemType.direction}" />
                </if>
                <else>
                    <return error="true"
                        message="one of fields itemTypeId/company/direction/accountcode ${itemType.itemTypeId}/${companyPartyId}/${itemType.direction}/${itemType.accountCode} is missing" />
                </else>
            </if>
            <if condition="update">
                <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                    list="accounts">
                    <econdition field-name="organizationPartyId" from="companyPartyId" />
                    <econdition field-name="accountCode" from="itemType.accountCode" />
                </entity-find>
                <if condition="itemTypeGlAccount">
                    <!-- update -->
                    <set field="itemTypeGlAccount.glAccountId" from="accounts[0].glAccountId" />
                    <entity-update value-field="itemTypeGlAccount" />
                    <else>
                        <!-- add -->
                        <service-call name="create#mantle.ledger.config.ItemTypeGlAccount"
                            in-map="[itemTypeEnumId: itemType.itemTypeId,
                                    organizationPartyId: companyPartyId,
                                    direction: itemType.direction,
                                    glAccountId: accounts[0].glAccountId]" />
                    </else>
                </if>
                <set field="itemType.accountName" from="ec.l10n.localize(accounts[0].accountName)" />
            </if>
            <if condition="delete">
                <entity-delete value-field="itemTypeGlAccount" />
            </if>
        </actions>
    </service>

    <service verb="get" noun="PaymentType">
        <description>payment types for payment</description>
        <in-parameters>
            <parameter name="sales" type="Boolean" />
        </in-parameters>
        <out-parameters>
            <parameter name="paymentTypes" type="List">
                <parameter name="paymentType" type="Map">
                    <parameter name="paymentTypeId" />
                    <parameter name="paymentTypeName" />
                    <parameter name="accountCode" />
                    <parameter name="accountName" />
                    <parameter name="isPayable" type="Boolean" />
                    <parameter name="isApplied" type="Boolean" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <set field="paymentTypes" from="[]" />
            <entity-find entity-name="moqui.basic.EnumerationAndType" list="enumPaymentTypes">
                <econdition field-name="enumTypeId" value="PaymentType" />
            </entity-find>
            <if condition="sales != null">
                <set field="isPayable" from="sales == true ? 'N' : 'Y'" />
            </if>
            <entity-find entity-name="mantle.ledger.config.PaymentTypeAndGlAccount"
                list="paymentTypeList">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="isPayable" from="isPayable" ignore-if-empty="true" />
                <order-by field-name="accountCode,typeDescription" />
            </entity-find>
            <iterate list="[true, false]" entry="payable">
                <set field="isPayable" from="payable" type="Boolean" />
                <iterate list="[true, false]" entry="applied">
                    <set field="isApplied" from="applied" type="Boolean" />
                    <iterate list="enumPaymentTypes" entry="paymentType">
                        <script>
                            var newPaymentType = paymentTypeList.find{
                                item -&gt; item.paymentTypeEnumId == paymentType.enumId
                                &amp;&amp; item.isPayable == (isPayable ? 'Y' : 'N')
                                &amp;&amp; item.isApplied == (isApplied ? 'Y' : 'N')
                            }
                        </script>
                        <if
                            condition="(sales != null &amp;&amp; newPaymentType != null) || sales == null">
                            <set field="paymentTypeOut"
                                from="[paymentTypeId: paymentType.enumId,
                                paymentTypeName: paymentType.description,
                                isApplied: applied,
                                isPayable: payable,
                                accountCode: newPaymentType?.accountCode,
                                accountName: newPaymentType?.accountName ? ec.l10n.localize(newPaymentType.accountName) : null,
                                ]" />
                            <script>paymentTypes.add(paymentTypeOut)</script>
                        </if>
                    </iterate>
                </iterate>
            </iterate>
            <script>paymentTypes.sort{it.paymentTypeName + it.isPayable + it.isApplied}</script>
        </actions>
    </service>

    <service verb="update" noun="PaymentType">
        <description>itemtypes for orderItem and invoiceItem either sales or purchase</description>
        <in-parameters>
            <parameter name="paymentType" type="Map">
                <parameter name="paymentTypeId" />
                <parameter name="paymentTypeName" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="isPayable" type="Boolean" />
                <parameter name="isApplied" type="Boolean" />
            </parameter>
            <parameter name="update" type="Boolean" />
            <parameter name="delete" type="Boolean" />
        </in-parameters>
        <out-parameters>
            <parameter name="paymentType" type="Map">
                <parameter name="paymentTypeId" />
                <parameter name="paymentTypeName" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="isPayable" type="Boolean" />
                <parameter name="isApplied" type="Boolean" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if
                condition="paymentType.paymentTypeId != null &amp;&amp; paymentType.isPayable != null &amp;&amp; paymentType.isApplied != null &amp;&amp; paymentType.accountCode != null">
                <entity-find-one entity-name="mantle.ledger.config.PaymentTypeGlAccount"
                    value-field="paymentTypeGlAccount" for-update="true">
                    <field-map field-name="paymentTypeEnumId" from="paymentType.paymentTypeId" />
                    <field-map field-name="organizationPartyId" from="companyPartyId" />
                    <field-map field-name="isPayable"
                        from="paymentType.isPayable == true ? 'Y' : 'N'" />
                    <field-map field-name="isApplied"
                        from="paymentType.isApplied == true ? 'Y' : 'N'" />
                </entity-find-one>
                <if condition="!paymentTypeGlAccount &amp;&amp; delete">
                    <return error="true"
                        message="Could not find paymentTypeId/company/direction ${paymentType.paymentTypeId}/${companyPartyId}/${paymentType.direction}" />
                </if>
                <else>
                    <return error="true"
                        message="one of paymentTypeId/isPayable/isApplied/accountcode ${paymentType.paymentTypeId}/${paymentType.isPayable}/${paymentType.isApplied}/${paymentType.accountCode} missing" />
                </else>
            </if>
            <if condition="update">
                <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                    list="accounts">
                    <econdition field-name="organizationPartyId" from="companyPartyId" />
                    <econdition field-name="accountCode" from="paymentType.accountCode" />
                </entity-find>
                <if condition="paymentTypeGlAccount">
                    <!-- update -->
                    <set field="paymentTypeGlAccount.glAccountId" from="accounts[0].glAccountId" />
                    <entity-update value-field="paymentTypeGlAccount" />
                    <else>
                        <!-- add -->
                        <service-call name="create#mantle.ledger.config.PaymentTypeGlAccount"
                            in-map="[paymentTypeEnumId: paymentType.paymentTypeId,
                                    organizationPartyId: companyPartyId,
                                    isPayable: paymentType.isPayable == true ? 'Y' : 'N',
                                    isApplied: paymentType.isApplied == true ? 'Y' : 'N',
                                    glAccountId: accounts[0].glAccountId]" />
                    </else>
                </if>
                <set field="paymentType.accountName" from="ec.l10n.localize(accounts[0].accountName)" />
            </if>
            <if condition="delete">
                <entity-delete value-field="paymentTypeGlAccount" />
            </if>
        </actions>
    </service>

    <service verb="get" noun="LedgerJournal">
        <in-parameters>
            <parameter name="journalId" />
            <parameter name="isPosted" type="Boolean" />
            <parameter name="start" default-value="0" type="Integer" />
            <parameter name="limit" default-value="10" type="Integer" />
            <parameter name="search" />
        </in-parameters>
        <out-parameters>
            <parameter name="ledgerJournals" type="List">
                <parameter name="journalId" />
                <parameter name="journalName" />
                <parameter name="isError" type="Boolean" />
                <parameter name="postedDate" />
                <parameter name="isPosted" type="Boolean" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if condition="search">
                <set field="search" value="%$search%" />
            </if>
            <entity-find entity-name="mantle.ledger.transaction.GlJournal"
                limit="limit" offset="start" list="ledgerJournalList">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="glJournalId"
                    from="journalId" ignore-if-empty="true" />
                <econdition field-name="isPosted" from="isPosted" ignore-if-empty="true" />
                <econditions combine="or">
                    <econdition field-name="glJournalName" operator="like" ignore-case="true"
                        from="search" ignore-if-empty="true" />
                    <econdition field-name="glJournalId" operator="like" ignore-case="true"
                        from="search" ignore-if-empty="true" />
                </econditions>
            </entity-find>
            <set field="ledgerJournals" from="[]" />
            <iterate list="ledgerJournalList" entry="journal">
                <script>
                    ledgerJournals.add(
                        journalId: journal.glJournalId,
                        journalName: journal.glJournalName,
                        isError: journal.glJournalTypeEnumId == 'GLJRNL_ERROR'
                            ? true : false,
                        postedDate: journal.postedDate
                            ? ec.l10n.format(journal.postedDate,'yyyy-MM-dd') : null,
                        isPosted: journal.isPosted=='N' ? false : true,
                        )
                </script>
            </iterate>
        </actions>
    </service>

    <service verb="create" noun="LedgerJournal">
        <in-parameters>
            <parameter name="ledgerJournal" type="Map">
                <parameter name="journalName" required="true" />
                <parameter name="isError" type="Boolean" default="false" />
                <parameter name="postedDate" />
                <parameter name="isPosted" type="Boolean" default="false" />
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="ledgerJournal" type="Map">
                <parameter name="journalId" />
                <parameter name="journalName" />
                <parameter name="isError" type="Boolean" />
                <parameter name="postedDate" />
                <parameter name="isPosted" type="Boolean" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <service-call name="create#mantle.ledger.transaction.GlJournal"
                in-map="[   organizationPartyId: companyPartyId,
                            isPosted: 'N',
                            glJournalName: ledgerJournal.journalName,
                            glJournalTypeEnumId: ledgerJournal.error == true
                                ? 'GLJRNL_ERROR'
                                : 'GLJRNL_MANUAL',
                        ]"
                out-map="context" />
            <service-call name="growerp.100.AccountingServices100.get#LedgerJournal"
                in-map="[journalId: glJournalId]" out-map="context" />
            <set field="ledgerJournal" from="ledgerJournals[0]" />
        </actions>
    </service>

    <service verb="update" noun="LedgerJournal">
        <in-parameters>
            <parameter name="ledgerJournal" type="Map">
                <parameter name="journalId" required="true" />
                <parameter name="journalName" required="true" />
                <parameter name="isError" type="Boolean" default="false" />
                <parameter name="postedDate" />
                <parameter name="isPosted" type="Boolean" default="false" />
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="ledgerJournal" type="Map">
                <parameter name="journalId" />
                <parameter name="journalName" />
                <parameter name="isError" type="Boolean" />
                <parameter name="postedDate" />
                <parameter name="isPosted" type="Boolean" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <service-call name="growerp.100.AccountingServices100.get#LedgerJournal"
                in-map="[journalId: journalId]" out-map="old" />
            <if condition="ledgerJournal.journalName != old.ledgerJournals[0].journalName">
                <service-call name="update#mantle.ledger.transaction.GlJournal"
                    in-map="[
                        glJournalId: ledgerJournal.journalId,
                        glJournalName: ledgerJournal.journalName,
                    ]" />
            </if>
            <if
                condition="ledgerJournal.isPosted &amp;&amp;
                ledgerJournal.isPosted != old.ledgerJournals[0].isPosted">
                <service-call name="mantle.ledger.LedgerServices.post#GlJournal"
                    in-map="[glJournalId: ledgerJournal.journalId]" />
            </if>
            <service-call name="growerp.100.AccountingServices100.get#LedgerJournal"
                in-map="[journalId: ledgerJournal.journalId]" out-map="context" />
            <set field="ledgerJournal" from="ledgerJournals[0]" />
        </actions>
    </service>

    <service verb="get" noun="GlAccount">
        <in-parameters>
            <parameter name="glAccountId" />
            <parameter name="start" default-value="0" type="Integer" />
            <parameter name="limit" default-value="10" type="Integer" />
            <parameter name="search" />
            <parameter name="trialBalance" type="Boolean" default="false" />
        </in-parameters>
        <out-parameters>
            <parameter name="glAccounts" type="List">
                <parameter name="glAccountId" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="isDebit" type="Boolean" />
                <parameter name="accountClass" type="Map">
                    <parameter name="topClassId" />
                    <parameter name="topDescription" />
                    <parameter name="parentClassId" />
                    <parameter name="parentDescription" />
                    <parameter name="classId" />
                    <parameter name="description" />
                    <parameter name="detailClassId" />
                    <parameter name="detailDescription" />
                    <parameter name="contra" />
                </parameter>
                <parameter name="accountType" type="Map">
                    <parameter name="accountTypeId" />
                    <parameter name="description" />
                </parameter>
                <parameter name="beginningBalance" />
                <parameter name="postedDebits" />
                <parameter name="postedCredits" />
                <parameter name="postedBalance" />
            </parameter>
            <parameter name="initialUploadAllowed" type="Boolean">
                <description>False when the ledger holds transactions other than the initial
                    balance: the initial upload replaces the ledger and is refused then</description>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <set field="initialUploadAllowed" value="true" type="Boolean" />
            <if condition="companyPartyId">
                <service-call name="growerp.100.AccountingServices100.check#InitialUploadAllowed"
                    in-map="[organizationPartyId: companyPartyId]" out-map="uploadCheck" />
                <set field="initialUploadAllowed" from="uploadCheck.allowed" type="Boolean" />
            </if>
            <if condition="search">
                <set field="search" value="%$search%" />
            </if>
            <set field="glAccountCodeMask"
                from="partyAcctgPreference?.glAccountCodeMask ?: ec.user.getPreference('GlAccountCodeMask')" />
            <set field="accountCodePattern"
                from="glAccountCodeMask ? glAccountCodeMask.replaceAll('[^#]', '').replaceAll('#', '_') : null" />
            <if condition="trialBalance == true">
                <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                    list="glAccountList" offset="start">
                    <econdition field-name="organizationPartyId" from="companyPartyId" />
                    <econdition field-name="accountCode" operator="like"
                        from="accountCodePattern" ignore-if-empty="true" />
                    <econdition field-name="postedBalance" operator="not-equals"
                        from="BigDecimal.ZERO" />
                    <order-by field-name="accountCode" />
                </entity-find>
                <else>
                    <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                        limit="limit" offset="start" list="glAccountList">
                        <econdition field-name="organizationPartyId" from="companyPartyId" />
                        <econdition field-name="glAccountId"
                            from="glAccountId" ignore-if-empty="true" />
                        <econdition field-name="accountCode" operator="like"
                            from="accountCodePattern" ignore-if-empty="true" />
                        <econditions combine="or">
                            <econdition field-name="accountName" operator="like" ignore-case="true"
                                from="search" ignore-if-empty="true" />
                            <econdition field-name="accountCode" operator="like" ignore-case="true"
                                from="search" ignore-if-empty="true" />
                        </econditions>
                        <order-by field-name="accountCode" />
                    </entity-find>
                </else>
            </if>
            <set field="glAccounts" from="[]" />
            <set field="totalCredit" from="BigDecimal.ZERO" type="BigDecimal" />
            <set field="totalDebit" from="BigDecimal.ZERO" type="BigDecimal" />
            <iterate list="glAccountList" entry="glAccount">
                <!-- match on the class id, not on its description: Enumeration.description is a
                     localized field, so in another language it no longer equals what is stored -->
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econditions combine="or">
                        <econdition field-name="detailClassId"
                            from="glAccount.glAccountClassEnumId" />
                        <econdition field-name="classId" from="glAccount.glAccountClassEnumId" />
                        <econdition field-name="parentClassId"
                            from="glAccount.glAccountClassEnumId" />
                        <econdition field-name="topClassId" from="glAccount.glAccountClassEnumId" />
                    </econditions>
                    <order-by
                        field-name="[topDescription,parentDescription,description,detailDescription]" />
                </entity-find>
                <script><![CDATA[
                    // the account sits somewhere in the chain, and the levels below it belong to
                    // other classes: leave them out, or the dialog shows a deeper class than the
                    // account has and saves that back
                    def cls = classes[0]
                    String own = glAccount.glAccountClassEnumId
                    int level = own == cls.detailClassId ? 4 : own == cls.classId ? 3 :
                                own == cls.parentClassId ? 2 : 1
                    // the view aliases the Enumeration description, and an alias does not carry
                    // its enable-localization: translate the class names on the way out
                    def levelClass = [
                        topClassId: cls.topClassId ?: '',
                        topDescription: ec.l10n.localize(cls.topDescription ?: ''),
                        parentClassId: level >= 2 ? (cls.parentClassId ?: '') : '',
                        parentDescription: level >= 2 ?
                            ec.l10n.localize(cls.parentDescription ?: '') : '',
                        classId: level >= 3 ? (cls.classId ?: '') : '',
                        description: level >= 3 ? ec.l10n.localize(cls.description ?: '') : '',
                        detailClassId: level >= 4 ? (cls.detailClassId ?: '') : '',
                        detailDescription: level >= 4 ?
                            ec.l10n.localize(cls.detailDescription ?: '') : '',
                        contra: cls.detailContra == 'Y' ? true : false,
                    ]
                    glAccounts.add(
                        glAccountId: glAccount.glAccountId,
                        accountCode: glAccount.accountCode,
                        accountName: ec.l10n.localize(glAccount.accountName),
                        accountClass: levelClass,
                        accountType: [accountTypeId: glAccount.glAccountTypeEnumId,
                            description: ec.l10n.localize(glAccount.typeDescription ?: '')],
                        isDebit: glAccount.isDebit == 'Y' ? true : false,
                        postedBalance: glAccount.postedBalance?.toString(),
                        )
                    if (glAccount.postedBalance != null && trialBalance == true) {
                        if (glAccount.isDebit == 'Y') {
                            totalDebit += glAccount.postedBalance;
                        } else {
                            totalCredit += glAccount.postedBalance;
                        }
                    }
                ]]></script>
            </iterate>
            <if
                condition="trialBalance == true">
                <script>
                    glAccounts.add(
                        accountName: 'Total posted',
                        accountCode: '-----',
                        postedCredits: totalCredit?.toString(),
                        postedDebits: totalDebit?.toString(),
                    ); 
                </script>
            </if>
        </actions>
    </service>

    <service verb="create" noun="GlAccount">
        <in-parameters>
            <parameter name="glAccount" type="Map">
                <parameter name="accountCode" required="true" />
                <parameter name="accountName" />
                <parameter name="accountClass" type="Map" required="true">
                    <parameter name="classId" />
                    <parameter name="description" /><!-- classId/description from any level-->
                </parameter>
                <parameter name="accountType" type="Map">
                    <parameter name="accountTypeId" />
                    <parameter name="description" />
                </parameter>
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="glAccount" type="Map">
                <parameter name="glAccountId" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="isDebit" type="Boolean" />
                <parameter name="accountClass" type="Map">
                    <parameter name="topClassId" />
                    <parameter name="topDescription" />
                    <parameter name="parentClassId" />
                    <parameter name="parentDescription" />
                    <parameter name="classId" />
                    <parameter name="description" />
                    <parameter name="detailClassId" />
                    <parameter name="detailDescription" />
                </parameter>
                <parameter name="accountType" type="Map">
                    <parameter name="accountTypeId" />
                    <parameter name="description" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <if condition="!glAccount.accountClass">
                <return error="true"
                    message="Account class missing for account ${glAccount.accountCode}" />
            </if>
            <!-- get class from the id the app sends; a csv upload only has the description -->
            <service-call name="growerp.100.AccountingServices100.get#GlAccountClasses"
                in-map="[accountClass: glAccount.accountClass]" out-map="context" />
            <if condition="!classes">
                <return error="true"
                    message="accountId: $glAccount.accountCode has an invalid class: ${glAccount.accountClass?.description}" />
            </if>
            <!-- same for the type: the app sends the id, a csv upload only the description -->
            <set field="typeEnumId" from="glAccount.accountType?.accountTypeId" />
            <if condition="!typeEnumId &amp;&amp; glAccount.accountType?.description">
                <entity-find entity-name="moqui.basic.Enumeration" list="typeEnums" limit="1">
                    <econdition field-name="enumTypeId" value="GlAccountType" />
                    <econdition field-name="description" from="glAccount.accountType.description" />
                </entity-find>
                <if condition="!typeEnums">
                    <return error="true"
                        message="accountId: $glAccount.accountCode has an invalid type: ${glAccount.accountType.description}" />
                </if>
                <set field="typeEnumId" from="typeEnums[0].enumId" />
            </if>
            <service-call name="growerp.100.AccountingServices100.get#AccountParent"
                in-map="[accountCode: glAccount.accountCode]"
                out-map="context" />
            <!-- create account -->
            <service-call name="create#mantle.ledger.account.GlAccount"
                in-map="[   accountCode: glAccount.accountCode,
                            parentGlAccountId: parentGlAccountId,
                            accountName: glAccount.accountName,
                            glAccountClassEnumId: classEnumId,
                            glAccountTypeEnumId: typeEnumId,
                            isTemporary: 'N',
                            disallowPosting: 'N',
                        ]"
                out-map="context" />
            <service-call name="create#mantle.ledger.account.GlAccountOrganization"
                in-map="[   glAccountId: glAccountId,
                            organizationPartyId: companyPartyId,
                        ]" />
            <service-call name="growerp.100.AccountingServices100.get#GlAccount"
                in-map="[glAccountId: glAccountId]" out-map="context" />
            <set field="glAccount" from="glAccounts[0]" />
        </actions>
    </service>

    <service verb="get" noun="AccountParent">
        <description>Get the account code parent by just analysing the accountCode</description>
        <in-parameters>
            <parameter name="accountCode" />
        </in-parameters>
        <out-parameters>
            <parameter name="parentGlAccountId" />
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <script>
                def List glAccounts;
                def String newAccountCode;
                def lastNumber = 0;
                // get the last number in the code
                for(def x = accountCode.size()-1; x&gt;0; x--) {
                    if (accountCode[x] != '0') {
                        lastNumber = x
                        break;
                    }
                }
                while(!glAccounts &amp;&amp; lastNumber &gt; 0 ) {
                    newAccountCode = accountCode.substring(0, lastNumber);
                    newAccountCode += ('0' * (accountCode.size() - lastNumber));
                    glAccounts_xafind = ec.entity.find("growerp.ledger.account.GlAccountAndOrganization")
                    .condition((org.moqui.entity.EntityCondition) ec.entity.conditionFactory.makeActionConditionDirect("organizationPartyId", "equals", companyPartyId, null, null, false, false, false, "false")).condition((org.moqui.entity.EntityCondition) ec.entity.conditionFactory.makeActionConditionDirect("accountCode", "equals", newAccountCode, null, null, false, false, false, "false"))
                    glAccounts = glAccounts_xafind.list()
                    lastNumber--;
                }
                if (glAccounts)
                    parentGlAccountId = (glAccounts[0]?.glAccountId)    
            </script>
            <!--entity-find
            entity-name="growerp.ledger.account.GlAccountAndOrganization"
                list="glAccounts">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="accountCode" from="newAccountCode" />
            </entity-find>
            <set field="parentGlAccountId" from="glAccounts[0]?.glAccountId" /-->
        </actions>
    </service>

    <service verb="update" noun="GlAccount">
        <in-parameters>
            <parameter name="glAccount" type="Map">
                <parameter name="glAccountId" required="true" />
                <parameter name="accountCode" required="true" />
                <parameter name="accountName" />
                <parameter name="accountClass" type="Map" required="true">
                    <parameter name="classId" />
                    <parameter name="description" /><!-- classId/description from any level-->
                </parameter>
                <parameter name="accountType" type="Map">
                    <parameter name="accountTypeId" />
                </parameter>
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="glAccount" type="Map">
                <parameter name="glAccountId" />
                <parameter name="accountCode" />
                <parameter name="accountName" />
                <parameter name="isDebit" type="Boolean" />
                <parameter name="accountClass" type="Map">
                    <parameter name="topClassId" />
                    <parameter name="topDescription" />
                    <parameter name="parentClassId" />
                    <parameter name="parentDescription" />
                    <parameter name="classId" />
                    <parameter name="description" />
                    <parameter name="detailClassId" />
                    <parameter name="detailDescription" />
                </parameter>
                <parameter name="accountType" type="Map">
                    <parameter name="accountTypeId" />
                    <parameter name="description" />
                </parameter>
                <parameter name="beginningBalance" />
                <parameter name="postedDebits" />
                <parameter name="postedCredits" />
                <parameter name="postedBalance" />
            </parameter>
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />
            <service-call name="growerp.100.AccountingServices100.get#AccountParent"
                in-map="[accountCode: glAccount.accountCode]"
                out-map="context" />
            <!-- get class from the id the app sends; a csv upload only has the description -->
            <service-call name="growerp.100.AccountingServices100.get#GlAccountClasses"
                in-map="[accountClass: glAccount.accountClass]" out-map="context" />
            <if condition="!classes">
                <return error="true"
                    message="accountId: $glAccount.accountCode has an invalid class: ${glAccount.accountClass?.description}" />
            </if>
            <!-- the list and the dialog show the localized name, so an unchanged name comes back
                 translated: keep the stored original in that case, only a real edit overwrites it -->
            <entity-find-one entity-name="mantle.ledger.account.GlAccount" value-field="dbGlAccount">
                <field-map field-name="glAccountId" from="glAccount.glAccountId" />
            </entity-find-one>
            <set field="newAccountName" from="glAccount.accountName" />
            <if
                condition="dbGlAccount &amp;&amp; newAccountName == ec.l10n.localize(dbGlAccount.accountName)">
                <set field="newAccountName" from="dbGlAccount.accountName" />
            </if>
            <service-call name="update#mantle.ledger.account.GlAccount"
                in-map="[   glAccountId: glAccount.glAccountId,
                            parentGlAccountId: parentGlAccountId,
                            accountCode: glAccount.accountCode,
                            accountName: newAccountName,
                            glAccountClassEnumId: classEnumId,
                            glAccountTypeEnumId: glAccount.accountType.accountTypeId,
                        ]" />
            <service-call name="growerp.100.AccountingServices100.get#GlAccount"
                in-map="[glAccountId: glAccount.glAccountId]" out-map="context" />
            <set field="glAccount" from="glAccounts[0]" />
        </actions>
    </service>

    <service verb="get" noun="GlAccountClasses">
        <description>The class rows an account class refers to. The app sends the class id; only a
            csv upload has just the description, which can only be matched when it is in the
            language the classes are stored in: Enumeration.description is a localized field.</description>
        <in-parameters>
            <parameter name="accountClass" type="Map">
                <parameter name="classId" />
                <parameter name="description" />
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="classes" type="List" />
            <parameter name="classEnumId">
                <description>the class the account gets: the level that matched, not the deepest
                    level of the chain it happens to sit in</description>
            </parameter>
        </out-parameters>
        <actions>
            <if condition="accountClass?.classId">
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econditions combine="or">
                        <econdition field-name="detailClassId" from="accountClass.classId" />
                        <econdition field-name="classId" from="accountClass.classId" />
                        <econdition field-name="parentClassId" from="accountClass.classId" />
                        <econdition field-name="topClassId" from="accountClass.classId" />
                    </econditions>
                    <order-by
                        field-name="[topDescription,parentDescription,description,detailDescription]" />
                </entity-find>
                <if condition="classes">
                    <set field="classEnumId" from="accountClass.classId" />
                </if>
            </if>
            <!-- Enumeration.description is localized: an econdition compares the stored value,
                but reading the field back returns it in the user's language. So the level that
                matched cannot be found by comparing the read values against what was asked for -
                that only ever succeeded for classes with no translation, and every other account
                silently fell back to the top class (all accounts ending up plain Debit/Credit).
                Ask per level instead, deepest first, and let the econdition do the matching. -->
            <if condition="!classes &amp;&amp; accountClass?.description">
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econdition field-name="detailDescription" from="accountClass.description" />
                    <order-by field-name="detailDescription" />
                </entity-find>
                <if condition="classes">
                    <set field="classEnumId" from="classes[0].detailClassId" />
                </if>
            </if>
            <if condition="!classes &amp;&amp; accountClass?.description">
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econdition field-name="description" from="accountClass.description" />
                    <order-by field-name="description" />
                </entity-find>
                <if condition="classes">
                    <set field="classEnumId" from="classes[0].classId" />
                </if>
            </if>
            <if condition="!classes &amp;&amp; accountClass?.description">
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econdition field-name="parentDescription" from="accountClass.description" />
                    <order-by field-name="parentDescription" />
                </entity-find>
                <if condition="classes">
                    <set field="classEnumId" from="classes[0].parentClassId" />
                </if>
            </if>
            <if condition="!classes &amp;&amp; accountClass?.description">
                <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels"
                    list="classes">
                    <econdition field-name="topDescription" from="accountClass.description" />
                    <order-by field-name="topDescription" />
                </entity-find>
                <if condition="classes">
                    <set field="classEnumId" from="classes[0].topClassId" />
                </if>
            </if>
        </actions>
    </service>

    <service verb="get" noun="GlAccountClass">
        <in-parameters>
            <parameter name="search" />
            <parameter name="start" default-value="0" type="Integer" />
            <parameter name="limit" default-value="3" type="Integer" />
        </in-parameters>
        <out-parameters>
            <parameter name="accountClasses" type="List">
                <parameter name="accountClass" type="Map">
                    <parameter name="topClassId" />
                    <parameter name="topDescription" />
                    <parameter name="parentClassId" />
                    <parameter name="parentDescription" />
                    <parameter name="classId" />
                    <parameter name="description" />
                    <parameter name="detailClassId" />
                    <parameter name="detailDescription" />
                </parameter>
            </parameter>
        </out-parameters>
        <actions>
            <if condition="search">
                <set field="search" value="%$search%" />
            </if>
            <entity-find entity-name="growerp.ledger.account.GlAccountClassAndLevels" list="enums"
                limit="limit" offset="start">
                <econditions combine="or">
                    <econdition field-name="topDescription" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                    <econdition field-name="parentDescription" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                    <econdition field-name="description" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                    <econdition field-name="detailDescription" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                </econditions>
                <order-by
                    field-name="[topDescription,parentDescription,description,detailDescription]" />
            </entity-find>
            <set field="accountClasses" from="[]" />
            <iterate list="enums" entry="enumEntry">
                <script>
                    // the view aliases the Enumeration description, and an alias does not carry
                    // its enable-localization: translate the class names on the way out
                    accountClasses.add(
                        topClassId: enumEntry.topClassId?:'',
                        topDescription: ec.l10n.localize(enumEntry.topDescription?:''),
                        parentClassId: enumEntry.parentClassId?:'',
                        parentDescription: ec.l10n.localize(enumEntry.parentDescription?:''),
                        accountClassId: enumEntry.classId?:'',
                        description: ec.l10n.localize(enumEntry.description?:''),
                        detailClassId: enumEntry.detailClassId?:'',
                        detailDescription: ec.l10n.localize(enumEntry.detailDescription?:''),
                    );
                </script>
            </iterate>
        </actions>
    </service>

    <service verb="get" noun="GlAccountType">
        <in-parameters>
            <parameter name="search" />
            <parameter name="start" default-value="0" type="Integer" />
            <parameter name="limit" default-value="2" type="Integer" />
        </in-parameters>
        <out-parameters>
            <parameter name="accountTypes" type="List">
                <parameter name="accountTypeId" />
                <parameter name="parentTypeId" />
                <parameter name="description" />
            </parameter>
        </out-parameters>
        <actions>
            <if condition="search">
                <set field="search" value="%$search%" />
            </if>
            <entity-find entity-name="moqui.basic.Enumeration" list="enums" limit="limit"
                offset="start">
                <econdition field-name="enumTypeId" value="GlAccountType" />
                <econditions combine="or">
                    <econdition field-name="description" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                    <econdition field-name="enumId" operator="like" from="search"
                        ignore-case="true" ignore-if-empty="true" />
                </econditions>
            </entity-find>
            <set field="accountTypes" from="[]" />
            <iterate list="enums" entry="enumEntry">
                <script>
                    accountTypes.add(
                        accountTypeId: enumEntry.enumId,
                        parentTypeId: enumEntry.parentEnumId,
                        description: enumEntry.description,
                    );
                </script>
            </iterate>
        </actions>
    </service>

    <service verb="close" noun="FinancialTimePeriod" transaction-timeout="1800"
        transaction="force-new">
        <in-parameters>
            <parameter name="timePeriodId" required="true" />
            <parameter name="closePrevious" type="Boolean" default="false" />
            <parameter name="closeChildren" type="Boolean" default="false" />
        </in-parameters>
        <actions>
            <entity-find-one entity-name="mantle.party.time.TimePeriod" value-field="timePeriod"
                for-update="true" />
            <service-call name="mantle.party.TimeServices.get#TimePeriodInfo"
                in-map="[timePeriod:timePeriod]" out-map="periodInfo" />
            <set field="fromTimestamp" from="periodInfo.fromTimestamp" />
            <set field="thruTimestamp" from="periodInfo.thruTimestamp" />
            <if condition="timePeriod.isClosed == 'Y'">
                <message error="true">Not closing period ${ec.resource.expand('TimePeriodNameTemplate','',timePeriod)}, already closed.</message>
            </if>
            <if condition="ec.user.nowTimestamp.before(timePeriod.thruDate)">
                <message error="true">Not closing period ${ec.resource.expand('TimePeriodNameTemplate','',timePeriod)}, period has not ended (ends on ${timePeriod.thruDate}).</message>
            </if>

            <!-- don't allow a period to close if the previous period is not closed -->
            <set field="previousTimePeriod" from="timePeriod.previous" />
            <if condition="previousTimePeriod &amp;&amp; previousTimePeriod.isClosed != 'Y'">
                <if condition="closePrevious">
                    <then>
                        <service-call
                            name="growerp.100.AccountingServices100.close#FinancialTimePeriod"
                            in-map="[timePeriodId:previousTimePeriod.timePeriodId, closePrevious:closePrevious, closeChildren:closeChildren]" />
                    </then>
                    <else>
                        <message error="true">Not closing period ${ec.resource.expand('TimePeriodNameTemplate','',timePeriod)}, previous period ${ec.resource.expand('TimePeriodNameTemplate','',previousTimePeriod)} not closed.</message>
                    </else>
                </if>
            </if>

            <!-- if any child periods make sure they are closed -->
            <entity-find entity-name="mantle.party.time.TimePeriod" list="childTimePeriodList">
                <econdition field-name="parentPeriodId" from="timePeriodId" />
                <order-by field-name="fromDate" />
            </entity-find>
            <iterate list="childTimePeriodList" entry="childTimePeriod">
                <if condition="childTimePeriod.isClosed != 'Y'">
                    <if condition="closeChildren">
                        <then>
                            <service-call
                                name="growerp.100.AccountingServices100.close#FinancialTimePeriod"
                                in-map="[timePeriodId:childTimePeriod.timePeriodId, closePrevious:closePrevious, closeChildren:closeChildren]" />
                        </then>
                        <else>
                            <message error="true">Not closing period ${ec.resource.expand('TimePeriodNameTemplate','',timePeriod)}, child period ${ec.resource.expand('TimePeriodNameTemplate','',childTimePeriod)} not closed.</message>
                        </else>
                    </if>
                </if>
            </iterate>

            <!-- make sure there are no unposted transactions within the time period -->
            <entity-find-count entity-name="growerp.account.AcctgTransAndCompany"
                count-field="unpostedTransCount">
                <econdition field-name="isPosted" operator="not-equals" value="Y" or-null="true" />
                <econdition field-name="companyPartyId" from="companyPartyId" />
                <econdition field-name="transactionDate" operator="greater-equals"
                    from="fromTimestamp" />
                <econdition field-name="transactionDate" operator="less-equals" from="thruTimestamp" />
            </entity-find-count>
            <if condition="unpostedTransCount &gt; 0">
                <message error="true">Not closing period ${ec.resource.expand('TimePeriodNameTemplate','',timePeriod)}, found ${unpostedTransCount} unposted transactions in period.</message>
            </if>

            <!-- check errors before continuing -->
            <check-errors />

            <!-- first close the TimePeriod to avoid further postings -->
            <set field="timePeriod.isClosed" value="Y" />
            <entity-update value-field="timePeriod" />

            <!-- recalculate postedDebits, postedCredits, endingBalance, etc on
            GlAccountOrgTimePeriod records for this time period -->
            <service-call
                name="mantle.ledger.LedgerServices.recalculate#GlAccountOrgTimePeriodAmounts"
                in-map="[timePeriodId:timePeriodId]" />

            <!-- a fiscal year that closes has to empty its profit and loss accounts into the
                net income account, without it their balances carry into the next year and the
                balance sheet stays out by the result of the year -->
            <if condition="timePeriod.timePeriodTypeId == 'FiscalYear'">
                <service-call
                    name="growerp.100.AccountingServices100.create#PeriodCloseAcctgTrans"
                    in-map="[timePeriodId: timePeriodId]" />
            </if>

            <message>Closed period ${timePeriod.periodName} and verified summary totals</message>
        </actions>
    </service>

    <service verb="create" noun="PeriodCloseAcctgTrans" transaction-timeout="1800">
        <description>Move the balances of the profit and loss accounts of a closed period into
            the net income account. Same purpose as
            mantle.ledger.LedgerServices.create#PeriodCloseAcctgTrans, but every query is
            limited to the organization: a single growerp database holds the ledgers of all
            tenants, so a lookup by account class alone returns another company's account.</description>
        <in-parameters>
            <parameter name="timePeriodId" required="true" />
            <parameter name="postTransactions" type="Boolean" default="true" />
            <parameter name="glJournalId" />
            <parameter name="netIncomeGlAccountId" />
        </in-parameters>
        <out-parameters>
            <parameter name="acctgTransIdByClassId" type="Map" />
        </out-parameters>
        <actions>
            <entity-find-one entity-name="mantle.party.time.TimePeriod" value-field="timePeriod" />
            <set field="organizationPartyId" from="timePeriod.partyId" />
            <!-- the owner of the company, for the transaction pseudoId sequence: take it from
                the company the period belongs to, not from the logged in user, so the service
                also runs from a scheduled job or from the tools service runner -->
            <entity-find-one entity-name="mantle.party.Party" value-field="organizationParty">
                <field-map field-name="partyId" from="organizationPartyId" />
            </entity-find-one>
            <set field="ownerPartyId" from="organizationParty?.ownerPartyId" />
            <service-call name="mantle.party.TimeServices.get#TimePeriodInfo"
                in-map="[timePeriod:timePeriod]" out-map="periodInfo" />
            <set field="transactionDate" from="periodInfo.thruTimestampLate" />

            <!-- the net income account of this company, not of whichever tenant happens to
                have the lowest account code -->
            <if condition="!netIncomeGlAccountId">
                <entity-find entity-name="growerp.ledger.account.GlAccountAndOrganization"
                    list="niGlAccounts">
                    <econdition field-name="organizationPartyId" />
                    <econdition field-name="glAccountClassEnumId" value="NET_INCOME" />
                    <order-by field-name="accountCode" />
                </entity-find>
                <set field="netIncomeGlAccountId"
                    from="niGlAccounts ? niGlAccounts[0].glAccountId : null" />
            </if>
            <if condition="!netIncomeGlAccountId">
                <return error="true"
                    message="The ledger of company ${organizationPartyId} has no account with class 'Net Income', cannot close period ${timePeriod.periodName}" />
            </if>

            <set field="acctgTransIdByClassId" from="[:]" />
            <set field="tempAccountClasses"
                from="['REVENUE', 'COST_OF_SALES', 'EXPENSE', 'INCOME', 'NON_OP_EXPENSE']" />
            <iterate list="tempAccountClasses" entry="tempAccountClass">
                <entity-find-one entity-name="moqui.basic.Enumeration" value-field="classEnum"
                    cache="true">
                    <field-map field-name="enumId" from="tempAccountClass" />
                </entity-find-one>
                <service-call name="org.moqui.impl.BasicServices.find#EnumerationByParent"
                    out-map="nestedEnumOut"
                    in-map="[parentEnumId:tempAccountClass, includeNested:true]" />
                <service-call name="mantle.ledger.LedgerServices.find#RootGlAccountClassEnum"
                    out-map="rootEnumOut" in-map="[glAccountClassEnumId:tempAccountClass]" />
                <set field="isDebit" from="'DEBIT'.equals(rootEnumOut.rootGlAccountClassEnumId)" />
                <if condition="rootEnumOut.isContra == 'Y'">
                    <set field="isDebit" from="!isDebit" />
                </if>

                <entity-find entity-name="mantle.ledger.report.GlAccountOrgTimePeriodDetail"
                    list="glAccountDetailList">
                    <econdition field-name="organizationPartyId" />
                    <econdition field-name="timePeriodId" />
                    <econdition field-name="glAccountClassEnumId" operator="in"
                        from="nestedEnumOut.enumIdSet" />
                    <select-field field-name="glAccountId,isDebit,endingBalance" />
                </entity-find>

                <!-- nothing to close when every account of the class is at zero -->
                <set field="hasEndingBalances" from="false" />
                <iterate list="glAccountDetailList" entry="glAccountDetail">
                    <if condition="glAccountDetail.endingBalance">
                        <set field="hasEndingBalances" from="true" />
                        <break />
                    </if>
                </iterate>
                <if condition="!hasEndingBalances">
                    <continue />
                </if>

                <service-call name="growerp.100.GeneralServices100.getNext#PseudoId"
                    in-map="[ownerPartyId: ownerPartyId, seqName: 'transaction']"
                    out-map="context" />
                <set field="txDescription"
                    value="${classEnum.description} closing for period ${timePeriod.periodName}" />
                <service-call name="mantle.ledger.LedgerServices.create#AcctgTrans"
                    out-map="createOut"
                    in-map="[   acctgTransTypeEnumId: 'AttNetIncomeClosing',
                                organizationPartyId: organizationPartyId,
                                transactionDate: transactionDate,
                                pseudoId: seqNum,
                                description: txDescription,
                                glJournalId: glJournalId]" />
                <set field="acctgTransId" from="createOut.acctgTransId" />
                <script>acctgTransIdByClassId.put(tempAccountClass, acctgTransId)</script>

                <!-- an entry reversing the balance of every account of the class -->
                <set field="balanceTotal" from="0.0" />
                <iterate list="glAccountDetailList" entry="glAccountDetail">
                    <if condition="!glAccountDetail.endingBalance">
                        <continue />
                    </if>
                    <set field="isCurDebit" from="glAccountDetail.isDebit == 'Y'" />
                    <service-call name="mantle.ledger.LedgerServices.create#AcctgTransEntry"
                        in-map="[   acctgTransId: acctgTransId,
                                    glAccountId: glAccountDetail.glAccountId,
                                    debitCreditFlag: (isCurDebit ? 'C' : 'D'),
                                    amount: glAccountDetail.endingBalance]" />
                    <!-- a contra account within the class moves the total the other way -->
                    <set field="balanceTotal"
                        from="isDebit != isCurDebit ? balanceTotal - glAccountDetail.endingBalance :
                                balanceTotal + glAccountDetail.endingBalance" />
                </iterate>

                <!-- balancing entry on the net income account -->
                <service-call name="mantle.ledger.LedgerServices.create#AcctgTransEntry"
                    in-map="[   acctgTransId: acctgTransId,
                                glAccountId: netIncomeGlAccountId,
                                debitCreditFlag: (isDebit ? 'D' : 'C'),
                                amount: balanceTotal]" />

                <message>Created transaction ${acctgTransId} for ${classEnum.description}, total balance ${ec.l10n.format(balanceTotal, '#,##0.00')}</message>

                <if condition="postTransactions">
                    <service-call name="mantle.ledger.LedgerServices.post#AcctgTrans"
                        in-map="[acctgTransId: acctgTransId]" />
                </if>
            </iterate>

            <!-- the closing entries change the balances of this period and of every period
                that holds it, and through those the opening balances of the next year -->
            <entity-find entity-name="mantle.party.time.TimePeriodAndType"
                list="otherTimePeriodList">
                <econdition field-name="timePeriodId" operator="not-equals" from="timePeriodId" />
                <econdition field-name="fromDate" operator="less-equals" from="timePeriod.thruDate" />
                <econdition field-name="thruDate" operator="greater-equals"
                    from="timePeriod.thruDate" />
                <econdition field-name="periodPurposeEnumId" value="Fiscal" />
                <econdition field-name="partyId" from="organizationPartyId" />
            </entity-find>
            <set field="recalcPeriodIds" from="[timePeriodId]" />
            <iterate list="otherTimePeriodList" entry="otherTimePeriod">
                <script>recalcPeriodIds.add(otherTimePeriod.timePeriodId)</script>
            </iterate>
            <iterate list="recalcPeriodIds" entry="recalcPeriodId">
                <!-- recalculate#GlAccountOrgTimePeriodAmounts only refreshes the summary rows
                    that are already there, and posting does not create them unless the company
                    runs a real time gl summary. Without a row for the net income account the
                    result of the year is left out of the summaries and the trial balance of the
                    period stays off by that amount. -->
                <entity-find-one entity-name="mantle.ledger.account.GlAccountOrgTimePeriod"
                    value-field="netIncomeSummary">
                    <field-map field-name="glAccountId" from="netIncomeGlAccountId" />
                    <field-map field-name="organizationPartyId" from="organizationPartyId" />
                    <field-map field-name="timePeriodId" from="recalcPeriodId" />
                </entity-find-one>
                <if condition="netIncomeSummary == null">
                    <service-call name="create#mantle.ledger.account.GlAccountOrgTimePeriod"
                        in-map="[   glAccountId: netIncomeGlAccountId,
                                    organizationPartyId: organizationPartyId,
                                    timePeriodId: recalcPeriodId,
                                    beginningBalance: 0.0, endingBalance: 0.0,
                                    postedDebits: 0.0, postedCredits: 0.0,
                                    postedDebitsNoClosing: 0.0, postedCreditsNoClosing: 0.0]" />
                </if>
                <service-call
                    name="mantle.ledger.LedgerServices.recalculate#GlAccountOrgTimePeriodAmounts"
                    in-map="[timePeriodId:recalcPeriodId]" />
            </iterate>
        </actions>
    </service>

    <service verb="checkOut" noun="OnePage" authenticate="anonymous-all">
        <description>service to pay an amount on a single html page to a growerp tenant</description>
        <in-parameters>
            <parameter name="productStoreId" required="true">
                <description>productStoreId of receiving company to send the payment to</description>
            </parameter>
            <parameter name="email" required="true" />
            <parameter name="firstName" />
            <parameter name="lastName" />
            <parameter name="companyName" />
            <parameter name="currencyId" />
            <parameter name="creditCardNumber" />
            <parameter name="creditCardType" />
            <parameter name="nameOnCard" />
            <parameter name="expireMonth" />
            <parameter name="expireYear" />
            <parameter name="cVC" />
            <parameter name="country" />
            <parameter name="couponCode" />
            <parameter name="amount" />
            <parameter name="product">
                <description>product/plan name selected on the pricing page</description>
            </parameter>
        </in-parameters>
        <out-parameters>
            <parameter name="result" type="Boolean" default-value="false" />
            <parameter name="resultMessage" default-value="Payment failed" />
            <parameter name="productStoreId" />
            <parameter name="amount" />
        </out-parameters>
        <actions>
            <!-- get productStore and company -->
            <entity-find-one entity-name="mantle.product.store.ProductStore"
                value-field="productStore" />
            <set field="companyPartyId" from="productStore.organizationPartyId" />
            <!-- receiving company base currency -->
            <entity-find-one entity-name="mantle.ledger.config.PartyAcctgPreference"
                value-field="acctgPref">
                <field-map field-name="organizationPartyId" from="companyPartyId" />
            </entity-find-one>
            <set field="baseCurrencyUomId" from="acctgPref?.baseCurrencyUomId ?: 'USD'" />
            <!-- get company owner-->
            <entity-find-one entity-name="mantle.party.Party" value-field="party">
                <field-map field-name="partyId" from="companyPartyId" />
            </entity-find-one>
            <set field="ownerPartyId" from="party.ownerPartyId" />
            <!-- find person from email -->
            <entity-find entity-name="growerp.party.OwnerPersonDetailAndCompany" list="partyIds">
                <econditions combine="or">
                    <econdition field-name="emailAddress" from="email" />
                    <econdition field-name="emailAddressCm" from="email" />
                </econditions>
                <econdition field-name="ownerPartyId" from="ownerPartyId" />
            </entity-find>
            <if condition="partyIds">
                <set field="userPartyId" from="partyIds[0].userPartyId" />
                <!-- if not found create person in owner as customer with email-->
                <else>
                    <service-call name="growerp.100.GeneralServices100.getNext#PseudoId"
                        in-map="[ownerPartyId: ownerPartyId, seqName: 'party']"
                        out-map="context" />
                    <service-call name="create#mantle.party.Party"
                        in-map="[pseudoId: seqNum, partyTypeEnumId:'PtyPerson',ownerPartyId: ownerPartyId]"
                        out-map="newPartyOut" />
                    <set field="userPartyId" from="newPartyOut.partyId" />
                    <service-call name="create#mantle.party.Person"
                        in-map="[
                            firstName: firstName,
                            lastName: lastName,
                            partyId: userPartyId]" />
                    <service-call name="create#mantle.party.PartyRole"
                        in-map="[partyId:userPartyId, roleTypeId: 'Customer']" />
                    <!-- create email -->
                    <service-call name="mantle.party.ContactServices.create#EmailAddress"
                        in-map="[emailAddress: email, partyId: userPartyId,
                             contactMechPurposeId:'EmailPrimary']" />
                </else>
            </if>
            <!-- process payment with credit card -->
            <!-- check if payment method exists -->
            <entity-find entity-name="mantle.account.method.PaymentMethod" list="pms">
                <econdition field-name="ownerPartyId" from="userPartyId" />
            </entity-find>
            <if condition="pms">
                <set field="paymentMethodId" from="pms[0].paymentMethodId" />
                <else>
                    <!-- save creditcard in user record -->
                    <set field="expireDate"
                        value="${expireMonth}/20${expireYear}" />
                    <set field="inMap"
                        from="[
                                paymentMethodTypeEnumId: 'PmtCreditCard',
                                ownerPartyId: userPartyId,
                                cardNumber: creditCardNumber,
                                creditCardTypeEnumId: ec.entity.find('moqui.basic.Enumeration')
                                        .condition('description', creditCardType).one().enumId,
                                expireDate: expireDate,
                                firstNameOnAccount: nameOnCard,
                                lastNameOnAccount: '',
                                paymentGatewayConfigId: System.getProperty('paymentGatewayConfigId'),
                                ]" />
                    <service-call
                        name="mantle.account.PaymentMethodServices.create#CreditCard"
                        in-map="inMap" out-map="userPayment" />
                    <set field="paymentMethodId" from="userPayment.paymentMethodId" />
                </else>
            </if>
            <!-- create payment -->
            <if condition="product">
                <set field="memo" value="GrowERP plan: ${product}" />
            </if>
            <!-- payment comes in as USD, convert to company base currency when different -->
            <set field="paymentAmount" from="amount" />
            <set field="paymentCurrencyUomId" value="USD" />
            <if condition="baseCurrencyUomId != 'USD'">
                <service-call name="growerp.100.GeneralServices100.convert#Currency"
                    in-map="[amount: amount, fromCurrencyUomId: 'USD',
                        toCurrencyUomId: baseCurrencyUomId]"
                    out-map="conversion" />
                <if condition="conversion.converted">
                    <set field="paymentAmount" from="conversion.convertedAmount" />
                    <set field="paymentCurrencyUomId" from="baseCurrencyUomId" />
                    <set field="originalCurrencyAmount" from="amount" />
                    <set field="originalCurrencyUomId" value="USD" />
                </if>
            </if>
            <service-call name="growerp.100.GeneralServices100.getNext#PseudoId"
                in-map="[ownerPartyId: ownerPartyId, seqName: 'payment']" out-map="context" />
            <service-call name="create#mantle.account.payment.Payment"
                in-map="[   statusId: 'PmntPromised',
                            pseudoId: seqNum,
                            paymentType: 'PtPrePayment',
                            paymentMethodId: paymentMethodId,
                            amount: paymentAmount,
                            amountUomId: paymentCurrencyUomId,
                            originalCurrencyAmount: originalCurrencyAmount,
                            originalCurrencyUomId: originalCurrencyUomId,
                            paymentInstrumentEnumId: 'PiCreditCard',
                            effectiveDate: ec.user.nowTimestamp,
                            paymentTypeEnumId: 'PmtCreditCard',
                            fromPartyId: userPartyId,
                            toPartyId: companyPartyId,
                            memo: memo,
                            ]"
                out-map="paymentInfo" />
            <set field="paymentId" from="paymentInfo.paymentId" />
            <!-- authorizeAndCapture Payment -->
            <if condition="System.getProperty('paymentGatewayConfigId') == 'STRIPE'">
                <service-call name="Stripe.StripePaymentServices.authorizeAndCapture#Payment"
                    in-map="[paymentId: paymentId, 
                    paymentGatewayConfigId: System.getProperty('paymentGatewayConfigId')]" />
            </if>
            <set field="result" value="true" />
            <set field="resultMessage" value="Payment successful, thank you!" />
        </actions>
    </service>

    <!-- gateway services -->
    <service verb="action" noun="GatewayPayment">
        <in-parameters>
            <parameter name="paymentId" />
            <parameter name="action" />
        </in-parameters>
        <out-parameters>
            <parameter name="finDoc" />
        </out-parameters>
        <actions>
            <set field="paymentGatewayConfigId" from="System.getProperty('paymentGatewayConfigId')" />
            <!-- get service names -->
            <entity-find-one entity-name="mantle.account.method.PaymentGatewayConfig"
                value-field="paymentGatewayConfig" />
            <set field="actionService"
                from="[
                    authorize: paymentGatewayConfig.authorizeServiceName,
                    capture: paymentGatewayConfig.captureServiceName,
                    release: paymentGatewayConfig.releaseServiceName,
                ]" />
            <set field="service" from="actionService[action]" />
            <service-call name="${service}"
                in-map="[paymentId: paymentId, paymentGatewayConfigId: paymentGatewayConfigId]" />
            <service-call name="growerp.100.FinDocServices100.get#FinDoc" out-map="context"
                in-map="[finDocId: paymentId, docType: 'payment', sales: true]" />
            <set field="finDoc" from="finDocs[0]" />
        </actions>
    </service>
    <service verb="capture" noun="GatewayPayment">
        <in-parameters>
            <parameter name="paymentId" />
        </in-parameters>
        <out-parameters>
            <parameter name="finDoc" />
        </out-parameters>
        <actions>
            <service-call name="growerp.100.AccountingServices100.action#GatewayPayment"
                out-map="context"
                in-map="[paymentId: paymentId, action: 'capture']" />
        </actions>
    </service>
    <service verb="release" noun="GatewayPayment">
        <in-parameters>
            <parameter name="paymentId" />
        </in-parameters>
        <out-parameters>
            <parameter name="finDoc" />
        </out-parameters>
        <actions>
            <service-call name="growerp.100.AccountingServices100.action#GatewayPayment"
                out-map="context"
                in-map="[paymentId: paymentId, action: 'release']" />
        </actions>
    </service>
    <service verb="authorize" noun="GatewayPayment">
        <in-parameters>
            <parameter name="paymentId" />
        </in-parameters>
        <out-parameters>
            <parameter name="finDoc" />
        </out-parameters>
        <actions>
            <service-call name="growerp.100.AccountingServices100.action#GatewayPayment"
                out-map="context"
                in-map="[paymentId: paymentId, action: 'authorize']" />
        </actions>
    </service>

    <service verb="get" noun="LedgerDashboard">
        <description>Aggregates for the ledger dashboard tile: transaction
            mix by type (sales/purchase invoice, incoming/outgoing payment)
            and posting counters, scoped to the owner company.</description>
        <out-parameters>
            <parameter name="stageSummary" type="List" />
            <parameter name="accounts" type="Integer" />
            <parameter name="posted" type="Integer" />
            <parameter name="unposted" type="Integer" />
            <parameter name="totalTransactions" type="Integer" />
        </out-parameters>
        <actions>
            <service-call out-map="context"
                name="growerp.100.GeneralServices100.get#RelatedCompanyAndOwner" />

            <!-- transaction mix, fixed type order -->
            <entity-find-count count-field="salesInvoiceCount"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="acctgTransTypeEnumId" value="AttSalesInvoice" />
            </entity-find-count>
            <entity-find-count count-field="purchaseInvoiceCount"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="acctgTransTypeEnumId" value="AttPurchaseInvoice" />
            </entity-find-count>
            <entity-find-count count-field="incomingPaymentCount"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="acctgTransTypeEnumId" value="AttIncomingPayment" />
            </entity-find-count>
            <entity-find-count count-field="outgoingPaymentCount"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="acctgTransTypeEnumId" value="AttOutgoingPayment" />
            </entity-find-count>
            <set field="stageSummary" from="[
                [stageId: 'AttSalesInvoice', stageName: 'Sales Inv', count: salesInvoiceCount],
                [stageId: 'AttPurchaseInvoice', stageName: 'Purch Inv', count: purchaseInvoiceCount],
                [stageId: 'AttIncomingPayment', stageName: 'Incoming', count: incomingPaymentCount],
                [stageId: 'AttOutgoingPayment', stageName: 'Outgoing', count: outgoingPaymentCount]]" />

            <!-- posting / chart-of-accounts counters -->
            <entity-find-count count-field="accounts"
                entity-name="growerp.ledger.account.GlAccountAndOrganization">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
            </entity-find-count>
            <entity-find-count count-field="posted"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="isPosted" value="Y" />
            </entity-find-count>
            <entity-find-count count-field="unposted"
                entity-name="mantle.ledger.transaction.AcctgTrans">
                <econdition field-name="organizationPartyId" from="companyPartyId" />
                <econdition field-name="isPosted" operator="not-equals" value="Y" or-null="true" />
            </entity-find-count>
            <set field="totalTransactions" from="posted + unposted" />
        </actions>
    </service>
</services>