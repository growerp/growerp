name: backend
description: Backend package for GrowERP flutter frontend
version: 0.0.1
author:
homepage:
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^3.0.9
  shared_preferences: ^2.0.5
  global_configuration: ^1.6.0
  http: ^0.12.2
  models:
#    path: ../models
    git:
      url: https://github.com/growerp/growerp.git
      ref: packages
      path: models

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.2
  very_good_analysis: ^2.0.0

flutter:

