name: growerp_models
resolution: workspace
version: 1.12.2
description: Freezed data models and a Retrofit REST client for the GrowERP Moqui backend, shared across all GrowERP Flutter packages.
homepage: https://www.growerp.com
repository: https://github.com/growerp/growerp
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.10.0

dependencies:
  decimal: ^3.2.4
  equatable: ^2.0.5
  freezed_annotation: ^3.1.0
  dio: ^5.4.3
  retrofit: ^4.9.2
  pretty_dio_logger: ^1.3.1
  fast_csv: ^0.2.6
  logger: ^2.0.2+1
  universal_io: ^2.2.2
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^6.0.0
  build_runner: ^2.4.12
  json_serializable: ^6.8.0
  freezed: ^3.2.0
  retrofit_generator: ^10.2.3
