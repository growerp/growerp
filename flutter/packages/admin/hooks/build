#!/bin/bash

# create version tag
ls
echo "path: $DOCKERFILE_PATH tag: $DOCKER_TAG"
ls ../../../flutter
docker build \
    --build-arg DOCKER_TAG=$DOCKER_TAG \
    -t $IMAGE_NAME \
    -f $DOCKERFILE_PATH .
